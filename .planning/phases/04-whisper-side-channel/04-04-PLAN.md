---
phase: "04-whisper-side-channel"
plan: 04
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - pulse-client/src/renderer/components/WhisperPanel.vue
  - pulse-client/src/renderer/views/RoomView.vue
autonomous: true
requirements:
  - WHISP-04
  - WHISP-01
  - WHISP-03

must_haves:
  truths:
    - "Whisper panel tab is visible in the sidebar when connected to a room"
    - "Empty state is shown when no whisper groups exist (per D-18)"
    - "Each group card shows name, visibility badge, member list (per visibility mode), and speaking indicators"
    - "Speaking ring animates on group members who are actively speaking in that whisper room"
    - "Member sees transmit mode selector (both / ptt / whisper-only) per group"
    - "Member sees PTT key-capture input; binding is saved and loaded via pulseApi IPC"
    - "whisper-ptt:keydown IPC fires setMicrophoneEnabled(true) on correct whisper Room; keyup fires setMicrophoneEnabled(false)"
    - "whisperOnly transmit mode disables main room mic and enables whisper room mic immediately on mode change"
    - "Admin sees Create Group form (name, visibility, groupId fields), per-card Add Member input, per-member Remove button, and Dissolve button"
    - "Non-admin members see cards without create/edit/dissolve controls"
    - "Tab switcher allows switching between Participants and Whisper panels"
  artifacts:
    - path: "pulse-client/src/renderer/components/WhisperPanel.vue"
      provides: "Whisper panel sidebar component"
      contains: "WhisperPanel"
    - path: "pulse-client/src/renderer/views/RoomView.vue"
      provides: "Tab switcher between Participants and Whisper"
      contains: "sideTab"
  key_links:
    - from: "WhisperPanel.vue"
      to: "useWhisperStore"
      via: "const whisperStore = useWhisperStore()"
      pattern: "useWhisperStore"
    - from: "WhisperPanel.vue speaking indicator"
      to: "whisperStore.speakers"
      via: "whisperStore.speakers.get(group.groupId)"
      pattern: "speakers.get"
---

<objective>
Build the WhisperPanel.vue component (group cards, speaking indicators, transmit mode selector, PTT key-capture, admin create/edit controls) and update RoomView.vue to host a tab switcher between ParticipantPanel and WhisperPanel.

Purpose: Surfaces whisper group state and controls to all users (WHISP-04) and provides admin the group management UI (WHISP-01). Speaking indicators and transmit mode complete WHISP-03 end-to-end in the UI.
Output: WhisperPanel.vue and updated RoomView.vue with tab switcher sidebar.
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-whisper-side-channel/04-CONTEXT.md
@.planning/phases/04-whisper-side-channel/04-RESEARCH.md
@.planning/phases/04-whisper-side-channel/04-03-SUMMARY.md

<interfaces>
From pulse-client/src/renderer/components/ParticipantPanel.vue (VERIFIED — style tokens to reuse):
  CSS variables in use: var(--c-side), var(--c-border), var(--c-border-2), var(--c-side-2), var(--accent),
    var(--c-ink), var(--c-ink-2), var(--c-ink-4), var(--c-ink-5), var(--voice), var(--voice-soft),
    var(--live), var(--warn), var(--radius), var(--radius-sm)
  Component width: 220px, flex: 0 0 220px, border-left: 1px solid var(--c-border)
  Speaking ring animation class: sq-speaking-ring (border: 2px solid var(--voice); animation: ring-pulse)

From pulse-client/src/renderer/views/RoomView.vue (VERIFIED):
  Current sidebar: <ParticipantPanel :active-speakers="activeSpeakers" @toggle-priority-speaker="..." />
  Layout class .voice-view: display: flex
  The ParticipantPanel is a direct child — replace with .side-tabs wrapper

From useWhisperStore (Plan 03 output):
  groups: Ref<WhisperGroupEntry[]>
    WhisperGroupEntry: { groupId, name, visibility, isMember, memberUserIds?, memberCount? }
  speakers: Ref<Map<string, string[]>>    // groupId -> speaking userId[]
  myGroups: ComputedRef<WhisperGroupEntry[]>
  isAdmin: ComputedRef<boolean>
  setGroups, removeGroup, setSpeakers, clearSpeakers, setIsAdmin

From usePresence (Plan 03 output):
  createWhisperGroup(groupId, name, visibility): Promise<void>
  addWhisperMember(groupId, targetUserId): Promise<void>
  removeWhisperMember(groupId, targetUserId): Promise<void>
  dissolveWhisperGroup(groupId): Promise<void>

From useLiveKit (Plan 02 output):
  getWhisperRoom(groupId): Room | undefined
  — Used for mic publishing in transmit modes

From window.pulseApi (preload, Plan 03 output):
  setWhisperPttKey(groupId, accelerator | null): Promise<boolean>
  getWhisperPttKey(groupId): Promise<string | null>
  setWhisperTransmitMode(groupId, mode): Promise<boolean>
  getWhisperTransmitMode(groupId): Promise<string>
  onWhisperPttKeyDown(cb): void
  onWhisperPttKeyUp(cb): void
  removeWhisperPttListeners(): void

Admin check: whisperStore.isAdmin (from Plan 03 useWhisperStore)
Avatar helpers: avatarColor(name) and initials(name) — copy from RoomView.vue (same implementation)

Transmit mode values: 'both' | 'ptt' | 'whisperOnly'  (per D-14)
Visibility badge labels: hidden -> 'HIDDEN', existence -> 'VISIBLE', full -> 'PUBLIC'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhisperPanel.vue component</name>
  <files>pulse-client/src/renderer/components/WhisperPanel.vue</files>
  <read_first>
    - pulse-client/src/renderer/components/ParticipantPanel.vue (full — style tokens, avatar helpers, speaking ring animation to replicate)
    - pulse-client/src/renderer/stores/whisper.ts (WhisperGroupEntry interface and store API)
    - .planning/phases/04-whisper-side-channel/04-CONTEXT.md (D-19, D-20, D-21, D-22)
    - .planning/phases/04-whisper-side-channel/04-RESEARCH.md (Architecture Patterns — WhisperPanel.vue section)
  </read_first>
  <action>
    Create a new Single File Component at pulse-client/src/renderer/components/WhisperPanel.vue.

    TEMPLATE STRUCTURE:
    Root element: div.whisper-panel (v-if="roomStore.currentRoomName")
    Sections:
      1. Header (.whisper-head): text "Whisper"
      2. Admin create-group form (.whisper-create, v-if="whisperStore.isAdmin"):
           - Input for groupId (alphanumeric/hyphens, placeholder "group-id")
           - Input for name (placeholder "Group name")
           - Select for visibility: options hidden, existence, full (labels "Hidden", "Exists", "Public")
           - Submit button "Create" calling handleCreate()
           - Error message div (v-if="createError")
      3. Empty state (v-if="whisperStore.groups.length === 0"):
           - Text "No whisper groups." in muted ink color
      4. Group list (.whisper-list):
           v-for="group in whisperStore.groups" :key="group.groupId"
           Each card (.whisper-card):
             a. Card header row (.wc-head):
                  - Group name span (.wc-name)
                  - Visibility badge span (.wc-vis-badge) — class based on group.visibility
                    'hidden' -> class wc-vis--hidden, text "HIDDEN"
                    'existence' -> class wc-vis--existence, text "EXISTS"
                    'full' -> class wc-vis--full, text "PUBLIC"
                  - Admin controls (v-if="whisperStore.isAdmin"):
                      Edit button (opens inline edit; implement as simple toggle showing dissolve confirm)
                      Dissolve button calling handleDissolve(group.groupId) with confirm dialog
             b. Member list (.wc-members):
                  v-if="group.memberUserIds && group.memberUserIds.length > 0"
                  v-for="userId in group.memberUserIds" :key="userId"
                  Each member row (.wc-member):
                    - Avatar span (.wc-av) with background: avatarColor(userId) and text: initials(userId)
                    - Speaking ring span (.wc-speaking-ring, v-if isSpeaking(group.groupId, userId))
                    - userId span (.wc-member-name)
             c. Existence-only member count (v-else-if="group.visibility === 'existence' && group.memberCount !== undefined"):
                  Text "{{ group.memberCount }} member(s)"
             d. Admin member management (v-if="whisperStore.isAdmin"):
                  Per-card "Add member" row (.wc-add-member):
                    - Text input (placeholder "userId") bound to addMemberInput[group.groupId]
                    - Button "Add" calling handleAddMember(group.groupId)
                  Per existing member row — add "×" remove button (v-if="whisperStore.isAdmin")
                    calling handleRemoveMember(group.groupId, userId)

             e. Member controls (v-if="group.isMember"):
                  Transmit mode selector (.wc-transmit):
                    <select v-model="transmitModes[group.groupId]" @change="handleTransmitChange(group.groupId)">
                      <option value="both">Both (always on)</option>
                      <option value="ptt">PTT only</option>
                      <option value="whisperOnly">Whisper only</option>
                    </select>
                  PTT key binding row (v-if="transmitModes[group.groupId] === 'ptt'"):
                    - Label "PTT Key:"
                    - Span showing current binding or "Click to bind" (.wc-key-display)
                    - Key capture: on click, set capturingFor = group.groupId;
                      listen for next keydown in window, call handleKeyCapture(group.groupId, e.code)
                    - Clear button (if binding exists) calling clearPttKey(group.groupId)

    SCRIPT SETUP (TypeScript):
    Imports: ref, onMounted, onUnmounted from 'vue'; useWhisperStore from '../stores/whisper';
             useRoomStore from '../stores/room'; usePresence from '../composables/usePresence';
             useLiveKit from '../composables/useLiveKit';
             codeToAccelerator from '../composables/usePtt'  ← TOP-LEVEL import (not inline)

    Reactive state:
      const whisperStore = useWhisperStore()
      const roomStore = useRoomStore()
      const { createWhisperGroup, addWhisperMember, removeWhisperMember, dissolveWhisperGroup } = usePresence()
      const { getWhisperRoom, mainRoom } = useLiveKit()

      const createGroupId = ref('')
      const createName = ref('')
      const createVisibility = ref<'hidden'|'existence'|'full'>('hidden')
      const createError = ref('')
      const capturingFor = ref<string | null>(null)
      const transmitModes = ref<Record<string, string>>({})
      const pttBindings = ref<Record<string, string | null>>({})
      const addMemberInput = ref<Record<string, string>>({})  // groupId → userId input value

    isSpeaking(groupId, userId): boolean:
      return (whisperStore.speakers.get(groupId) ?? []).includes(userId)

    handleCreate():
      createError.value = ''
      if (!/^[a-zA-Z0-9-]+$/.test(createGroupId.value)) { createError.value = 'ID: alphanumeric and hyphens only'; return }
      if (!createName.value.trim()) { createError.value = 'Name required'; return }
      createWhisperGroup(createGroupId.value.trim(), createName.value.trim(), createVisibility.value)
        .catch(e => { createError.value = String(e) })
      createGroupId.value = ''; createName.value = ''

    handleDissolve(groupId):
      if (!confirm(`Dissolve group "${groupId}"?`)) return
      dissolveWhisperGroup(groupId)

    handleAddMember(groupId):
      const userId = (addMemberInput.value[groupId] ?? '').trim()
      if (!userId) return
      addWhisperMember(groupId, userId).catch(e => { createError.value = String(e) })
      addMemberInput.value[groupId] = ''

    handleRemoveMember(groupId, userId):
      removeWhisperMember(groupId, userId).catch(e => { createError.value = String(e) })

    handleTransmitChange(groupId):
      const mode = transmitModes.value[groupId] ?? 'both'
      window.pulseApi.setWhisperTransmitMode(groupId, mode)
      // Apply audio routing immediately based on new mode:
      const whisperRoom = getWhisperRoom(groupId)
      if (mode === 'whisperOnly') {
        // Disable main room mic; whisper room mic stays on
        mainRoom.value?.localParticipant?.setMicrophoneEnabled(false)
        whisperRoom?.localParticipant?.setMicrophoneEnabled(true)
      } else if (mode === 'both') {
        // Both rooms transmit continuously
        mainRoom.value?.localParticipant?.setMicrophoneEnabled(true)
        whisperRoom?.localParticipant?.setMicrophoneEnabled(true)
      } else if (mode === 'ptt') {
        // PTT mode: whisper room mic off until keydown; main room normal
        mainRoom.value?.localParticipant?.setMicrophoneEnabled(true)
        whisperRoom?.localParticipant?.setMicrophoneEnabled(false)
      }

    Wire PTT keydown/keyup to audio (in onMounted):
      window.pulseApi.onWhisperPttKeyDown(async (groupId: string) => {
        const whisperRoom = getWhisperRoom(groupId)
        const mode = transmitModes.value[groupId] ?? 'both'
        if (mode !== 'ptt') return
        await whisperRoom?.localParticipant?.setMicrophoneEnabled(true)
        const suppressMain = await window.pulseApi.getWhisperSuppressMain(groupId)
        if (suppressMain) mainRoom.value?.localParticipant?.setMicrophoneEnabled(false)
      })
      window.pulseApi.onWhisperPttKeyUp(async (groupId: string) => {
        const whisperRoom = getWhisperRoom(groupId)
        const mode = transmitModes.value[groupId] ?? 'both'
        if (mode !== 'ptt') return
        await whisperRoom?.localParticipant?.setMicrophoneEnabled(false)
        mainRoom.value?.localParticipant?.setMicrophoneEnabled(true)  // restore main mic
      })

    Wire PTT cleanup (in onUnmounted):
      window.pulseApi.removeWhisperPttListeners()

    handleKeyCapture(groupId, code):
      // codeToAccelerator is imported at the TOP of <script setup> — not inline here
      // Add to top-level imports: import { codeToAccelerator } from '../composables/usePtt'
      const accelerator = codeToAccelerator(code)
      if (!accelerator) return
      window.pulseApi.setWhisperPttKey(groupId, accelerator).then(ok => {
        if (ok) pttBindings.value[groupId] = accelerator
        else createError.value = 'Key not bindable (unmappable key)'
      })
      capturingFor.value = null

    clearPttKey(groupId):
      window.pulseApi.setWhisperPttKey(groupId, null)
      pttBindings.value[groupId] = null

    onMounted(): load transmitModes and pttBindings for all groups from pulseApi:
      for (const group of whisperStore.groups) {
        window.pulseApi.getWhisperTransmitMode(group.groupId).then(m => { transmitModes.value[group.groupId] = m })
        window.pulseApi.getWhisperPttKey(group.groupId).then(k => { pttBindings.value[group.groupId] = k })
      }
      window.addEventListener('keydown', onCaptureKeydown)

    onUnmounted(): window.removeEventListener('keydown', onCaptureKeydown)

    onCaptureKeydown(e: KeyboardEvent):
      if (!capturingFor.value || e.repeat) return
      e.preventDefault()
      handleKeyCapture(capturingFor.value, e.code)

    avatarColor and initials: copy exact implementations from ParticipantPanel.vue (same AV_COLORS array, same hash logic).

    STYLES (scoped):
    .whisper-panel: width: 220px; flex: 0 0 220px; border-left: 1px solid var(--c-border); background: var(--c-side); display: flex; flex-direction: column; overflow: hidden
    .whisper-head: same as .squad-head in ParticipantPanel (padding, font-size, font-weight, uppercase, color, border-bottom)
    .whisper-list: flex: 1 1 auto; overflow-y: auto; padding: 8px 10px; scrollbar-width: thin; display: flex; flex-direction: column; gap: 8px
    .whisper-card: border: 1.5px solid var(--c-border); border-radius: var(--radius); padding: 10px 10px; background: var(--c-bg); display: flex; flex-direction: column; gap: 6px
    .whisper-card.is-member: border-color: var(--accent); background: rgba(var(--accent-rgb, 99,102,241), 0.05)
    .wc-head: display: flex; align-items: center; gap: 6px
    .wc-name: flex: 1 1 auto; font-size: 13px; font-weight: 700; color: var(--c-ink); overflow: hidden; text-overflow: ellipsis; white-space: nowrap
    .wc-vis-badge: font-size: 9px; font-weight: 700; letter-spacing: 0.04em; border-radius: 20px; padding: 1px 6px; border: 1.5px solid currentColor; flex: 0 0 auto
    .wc-vis--hidden: color: var(--c-ink-4)
    .wc-vis--existence: color: var(--warn, #f0a500)
    .wc-vis--full: color: var(--voice)
    .wc-members: display: flex; flex-direction: column; gap: 2px
    .wc-member: display: flex; align-items: center; gap: 6px; padding: 2px 0
    .wc-av-wrap: position: relative; flex: 0 0 auto
    .wc-av: width: 22px; height: 22px; border-radius: 50%; color: #fff; font-size: 9px; font-weight: 700; display: flex; align-items: center; justify-content: center
    .wc-speaking-ring: position: absolute; inset: -3px; border-radius: 50%; border: 2px solid var(--voice); animation: ring-pulse 1.2s ease-in-out infinite
    @keyframes ring-pulse: same as ParticipantPanel
    .wc-member-name: font-size: 11px; color: var(--c-ink-4)
    .wc-transmit: margin-top: 4px
    .wc-transmit select: width: 100%; background: var(--c-bg); border: 1px solid var(--c-border-2); border-radius: var(--radius-sm); color: var(--c-ink); font-size: 12px; padding: 3px 6px; font-family: inherit
    .wc-ptt-row: display: flex; align-items: center; gap: 6px; margin-top: 4px
    .wc-key-display: flex: 1 1 auto; font-size: 11px; font-family: monospace; background: var(--c-side-2); border: 1px solid var(--c-border); border-radius: var(--radius-sm); padding: 2px 6px; cursor: pointer; color: var(--c-ink-4)
    .wc-key-display.capturing: border-color: var(--accent); color: var(--accent)
    .wc-create: padding: 8px 10px; border-bottom: 1px solid var(--c-border); display: flex; flex-direction: column; gap: 4px
    .wc-create input, .wc-create select: width: 100%; background: var(--c-bg); border: 1px solid var(--c-border-2); border-radius: var(--radius-sm); color: var(--c-ink); font-size: 12px; padding: 4px 6px; font-family: inherit; box-sizing: border-box
    .wc-create-btn: align-self: flex-end; height: 26px; padding: 0 12px; border: none; border-radius: var(--radius-sm); background: var(--accent); color: #fff; font-size: 12px; font-weight: 700; font-family: inherit; cursor: pointer
    .wc-create-err: font-size: 11px; color: var(--live)
    .whisper-empty: padding: 24px 14px; text-align: center; font-size: 13px; color: var(--c-ink-4)
    Admin control buttons (.wc-admin-btns): margin-left: auto; display: flex; gap: 4px
    .wc-dissolve-btn, .wc-edit-btn: font-size: 10px; padding: 2px 6px; border: 1px solid var(--c-border); border-radius: var(--radius-sm); background: transparent; color: var(--c-ink-4); cursor: pointer; font-family: inherit
    .wc-dissolve-btn:hover: color: var(--live); border-color: var(--live)

    Add .is-member class to .whisper-card div using :class="{ 'is-member': group.isMember }".
  </action>
  <verify>
    <automated>cd /c/projetos/Hopin/pulse-client && npm run typecheck 2>&1 | grep "WhisperPanel\|whisper" | head -10</automated>
  </verify>
  <done>
    - WhisperPanel.vue exists at pulse-client/src/renderer/components/WhisperPanel.vue.
    - npm run typecheck reports 0 errors related to WhisperPanel.vue.
    - Component renders group cards with name, visibility badge, member list, speaking ring, transmit mode selector, PTT key-capture.
    - Admin create form and dissolve button present (gated by whisperStore.isAdmin).
    - Empty state shown when whisperStore.groups.length === 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tab switcher to RoomView.vue for Participants / Whisper panel</name>
  <files>pulse-client/src/renderer/views/RoomView.vue</files>
  <read_first>
    - pulse-client/src/renderer/views/RoomView.vue (full current file — read before editing)
    - pulse-client/src/renderer/components/WhisperPanel.vue (just created — verify import path)
    - .planning/phases/04-whisper-side-channel/04-RESEARCH.md (RoomView.vue Layout section)
  </read_first>
  <action>
    Make the following targeted changes to RoomView.vue:

    1. ADD IMPORT in script setup section:
         import WhisperPanel from '../components/WhisperPanel.vue'
       alongside the existing ParticipantPanel import.

    2. ADD reactive tab state in script setup:
         const sideTab = ref<'participants' | 'whisper'>('participants')
       alongside other existing refs.

    3. REPLACE the current sidebar element in the template.
       Current template (lines 147-151):
         <ParticipantPanel
           :active-speakers="activeSpeakers"
           @toggle-priority-speaker="togglePrioritySpeaker"
         />
       Replace with:
         <div class="side-tabs">
           <div class="side-tab-bar">
             <button
               class="side-tab-btn"
               :class="{ active: sideTab === 'participants' }"
               @click="sideTab = 'participants'"
             >Participants</button>
             <button
               class="side-tab-btn"
               :class="{ active: sideTab === 'whisper' }"
               @click="sideTab = 'whisper'"
             >Whisper</button>
           </div>
           <ParticipantPanel
             v-if="sideTab === 'participants'"
             :active-speakers="activeSpeakers"
             @toggle-priority-speaker="togglePrioritySpeaker"
           />
           <WhisperPanel v-else />
         </div>

    4. ADD scoped CSS for the tab switcher — append to existing <style scoped> block:
         .side-tabs {
           width: 220px;
           flex: 0 0 220px;
           display: flex;
           flex-direction: column;
           border-left: 1px solid var(--c-border);
           background: var(--c-side);
           overflow: hidden;
         }
         .side-tab-bar {
           display: flex;
           flex: 0 0 auto;
           border-bottom: 1px solid var(--c-border);
         }
         .side-tab-btn {
           flex: 1 1 50%;
           height: 36px;
           border: none;
           background: transparent;
           color: var(--c-ink-4);
           font-size: 11px;
           font-weight: 700;
           font-family: inherit;
           letter-spacing: 0.04em;
           text-transform: uppercase;
           cursor: pointer;
           border-bottom: 2px solid transparent;
           transition: color 0.1s, border-color 0.1s;
         }
         .side-tab-btn.active {
           color: var(--c-ink);
           border-bottom-color: var(--accent);
         }
         .side-tab-btn:hover:not(.active) {
           color: var(--c-ink-2);
         }

    5. NOTE: ParticipantPanel's own .squad-panel CSS has width: 220px; flex: 0 0 220px; border-left.
       When inside .side-tabs, the ParticipantPanel will inherit the parent's border-left.
       To avoid a double border, set ParticipantPanel to NOT have its own border-left when used inside side-tabs.
       SIMPLEST approach: The .side-tabs wrapper sets the border-left; ParticipantPanel.vue's own
       border-left: 1px solid var(--c-border) will double it visually.
       Fix by overriding in RoomView scoped CSS:
         .side-tabs :deep(.squad-panel) {
           border-left: none;
           width: 100%;
           flex: 1 1 auto;
         }
       Similarly for WhisperPanel:
         .side-tabs :deep(.whisper-panel) {
           border-left: none;
           width: 100%;
           flex: 1 1 auto;
         }

    Do NOT change any other parts of RoomView.vue.
    Do NOT change the existing PTT wiring, handleJoin, handleJoinRoom, or any script logic.
  </action>
  <verify>
    <automated>cd /c/projetos/Hopin/pulse-client && npm run typecheck 2>&1 | grep -E "error|Error" | head -10</automated>
  </verify>
  <done>
    - npm run typecheck reports 0 errors.
    - RoomView.vue imports WhisperPanel.
    - Template contains .side-tabs div wrapping both ParticipantPanel and WhisperPanel with v-if/v-else.
    - .side-tab-bar with two .side-tab-btn buttons present in template.
    - sideTab ref declared in script setup.
    - :deep overrides prevent double border.
    - No existing functionality removed or altered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer UI → usePresence | createWhisperGroup / dissolveWhisperGroup calls pass through SignalR; server enforces admin check |
| Key-capture input → IPC | User keystrokes captured in renderer; only mapped accelerators stored; unmappable keys rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Elevation of Privilege | Create group form — non-admin can call createWhisperGroup | accept | Server IsServerAdmin() gate rejects call; UI hides form from non-admin but server is authoritative |
| T-04-12 | Tampering | groupId from create form used in hub call | mitigate | Client validates ^[a-zA-Z0-9-]+ before invoking; server also sanitizes (Plan 01) |
| T-04-13 | Information Disclosure | Visibility badge reveals existence of hidden groups | accept | Server never sends hidden group entries to non-members in WhisperGroupsUpdated payload |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
1. TypeScript clean build:
   cd /c/projetos/Hopin/pulse-client && npm run typecheck 2>&1 | tail -5
   Expected: 0 errors

2. Grep — WhisperPanel component structure:
   grep -c "useWhisperStore\|whisperStore\|wc-speaking-ring\|transmitModes\|capturingFor" pulse-client/src/renderer/components/WhisperPanel.vue
   Expected: >= 5

3. Grep — tab switcher in RoomView:
   grep -c "sideTab\|side-tab-btn\|WhisperPanel" pulse-client/src/renderer/views/RoomView.vue
   Expected: >= 3

4. Grep — admin controls gated:
   grep "whisperStore.isAdmin" pulse-client/src/renderer/components/WhisperPanel.vue
   Expected: at least 1 match (v-if guard on create form and dissolve button)

5. Dev server start check (smoke):
   cd /c/projetos/Hopin/pulse-client && npm run build 2>&1 | tail -10
   Expected: Build completed without errors

6. Grep — speaking ring uses whisperStore.speakers:
   grep "speakers.get\|isSpeaking" pulse-client/src/renderer/components/WhisperPanel.vue
   Expected: >= 1 match
</verification>

<success_criteria>
- npm run build (or typecheck) produces 0 errors
- WhisperPanel.vue renders group cards with all required fields per D-20
- Visibility badges respect D-19 modes (hidden/existence/full)
- Admin controls (create, dissolve) visible only when whisperStore.isAdmin is true per D-21
- Speaking indicators driven by whisperStore.speakers per D-22
- Transmit mode selector (3 options per D-14) saves to electron-store via pulseApi
- PTT key-capture input works; unmappable keys are rejected with error message
- Tab switcher in RoomView.vue shows correct panel; .side-tabs maintains 220px width
- No regression in existing ParticipantPanel functionality (priority speaker button, speaking rings)
</success_criteria>

<output>
Create .planning/phases/04-whisper-side-channel/04-04-SUMMARY.md when done
</output>
