---
phase: "04-whisper-side-channel"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pulse.Server/Hubs/PresenceHub.cs
  - Pulse.Server/Services/ILiveKitService.cs
autonomous: true
requirements:
  - WHISP-01
  - WHISP-02

must_haves:
  truths:
    - "Admin can create a named whisper group via hub method; non-admin is rejected immediately"
    - "Admin can add a member to a group; member receives WhisperGroupMemberAdded with a LiveKit token"
    - "Admin can remove a member from a group; member receives WhisperGroupMemberRemoved"
    - "Admin can dissolve a group; all members receive WhisperGroupDissolved"
    - "A user connecting while already a member of active groups receives JoinWhisperGroups with tokens"
    - "WhisperGroupsUpdated is broadcast respecting visibility modes (hidden groups invisible to non-members)"
  artifacts:
    - path: "Pulse.Server/Hubs/PresenceHub.cs"
      provides: "Whisper hub methods + OnConnectedAsync override"
      contains: "CreateWhisperGroup"
    - path: "Pulse.Server/Services/ILiveKitService.cs"
      provides: "GetLiveKitHost() in interface"
      contains: "GetLiveKitHost"
  key_links:
    - from: "PresenceHub.CreateWhisperGroup"
      to: "_whisperGroups ConcurrentDictionary"
      via: "GetOrAdd / TryAdd"
      pattern: "_whisperGroups"
    - from: "PresenceHub.OnConnectedAsync"
      to: "_liveKitService.GenerateRoomToken"
      via: "foreach memberGroups"
      pattern: "GenerateRoomToken"
---

<objective>
Add the whisper group server layer to PresenceHub: in-memory group state, admin-gated CRUD hub methods, OnConnectedAsync override that pushes tokens to returning members, and per-recipient WhisperGroupsUpdated broadcasts that respect visibility modes.

Purpose: Provides the cryptographic audio isolation backbone — tokens are minted server-side so non-members can never join a whisper LiveKit room.
Output: Modified PresenceHub.cs with full whisper hub API; modified ILiveKitService.cs with GetLiveKitHost() on interface.
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-whisper-side-channel/04-CONTEXT.md
@.planning/phases/04-whisper-side-channel/04-RESEARCH.md

<interfaces>
From Pulse.Server/Services/LiveKitService.cs (VERIFIED):
- GenerateRoomToken(string roomName, string participantIdentity, string participantName): string
- GetLiveKitHost(): string   — already implemented, but NOT yet on ILiveKitService interface

From Pulse.Server/Services/ILiveKitService.cs (current — missing GetLiveKitHost):
- Verify the interface before editing; add GetLiveKitHost() if absent.

From Pulse.Server/Hubs/PresenceHub.cs (current constructor):
  public class PresenceHub(AppDbContext db) : Hub
  — Must become: PresenceHub(AppDbContext db, ILiveKitService liveKitService, IConfiguration configuration)
  — ILiveKitService registered as AddScoped in Program.cs (line 18) — injectable.

From Pulse.Server/Program.cs:
  builder.Services.AddScoped<ILiveKitService, LiveKitService>();  // already present

Claim extraction pattern (VERIFIED from existing hub methods):
  var userId = Context.User?.FindFirst(ClaimTypes.NameIdentifier)?.Value
               ?? Context.User?.FindFirst("sub")?.Value ?? "";
  var displayName = Context.User?.FindFirst("displayName")?.Value ?? "Unknown";

Admin check env var (per D-10):
  _configuration["Pulse:AdminUserId"] ?? Environment.GetEnvironmentVariable("PULSE_ADMIN_USER_ID")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose GetLiveKitHost on ILiveKitService interface</name>
  <files>Pulse.Server/Services/ILiveKitService.cs</files>
  <read_first>
    - Pulse.Server/Services/ILiveKitService.cs (current interface — check if GetLiveKitHost already declared)
    - Pulse.Server/Services/LiveKitService.cs (concrete implementation to match signature)
  </read_first>
  <action>
    Read ILiveKitService.cs. If GetLiveKitHost() is absent, add the declaration:
      string GetLiveKitHost();
    alongside the existing GenerateRoomToken declaration. Do not change any other members.
    LiveKitService already implements GetLiveKitHost() (line 22 of LiveKitService.cs), so no changes needed there.
  </action>
  <verify>
    <automated>cd /c/projetos/Hopin/Pulse.Server && dotnet build --no-restore -q 2>&1 | grep -i error | head -20</automated>
  </verify>
  <done>dotnet build produces no errors; ILiveKitService declares GetLiveKitHost().</done>
</task>

<task type="auto">
  <name>Task 2: Add whisper state, hub methods, and OnConnectedAsync to PresenceHub</name>
  <files>Pulse.Server/Hubs/PresenceHub.cs</files>
  <read_first>
    - Pulse.Server/Hubs/PresenceHub.cs (full current file — read every line before editing)
    - Pulse.Server/Services/ILiveKitService.cs (interface after Task 1)
    - .planning/phases/04-whisper-side-channel/04-RESEARCH.md (Critical Code Analysis §2, §3, §4; Pitfalls §4, §6)
  </read_first>
  <action>
    Perform the following changes to PresenceHub.cs:

    1. UPDATE CONSTRUCTOR — change primary constructor from (AppDbContext db) to:
         PresenceHub(AppDbContext db, ILiveKitService liveKitService, IConfiguration configuration)
       Store as private fields: _db = db, _liveKitService = liveKitService, _configuration = configuration.
       Because the current hub uses primary constructor syntax (AppDbContext db) with direct `db.` usage,
       convert to explicit constructor body or field assignment as needed so existing db calls still compile.

    2. ADD RECORD — after the ParticipantInfo record, add:
         private record WhisperGroup(
             string GroupId,
             string Name,
             string Visibility,
             System.Collections.Concurrent.ConcurrentBag<string> MemberUserIds,
             string LiveKitRoomName
         );

    3. ADD STATIC DICTIONARIES — after the existing _prioritySpeakers dictionary:
         private static readonly ConcurrentDictionary<string, WhisperGroup>
             _whisperGroups = new();
         private static readonly ConcurrentDictionary<string, ConcurrentBag<string>>
             _userToWhisperGroups = new();
         private static readonly ConcurrentDictionary<string, string>
             _userToConnection = new();

    4. ADD IsServerAdmin() HELPER — private bool method:
         private bool IsServerAdmin() {
             var callerUserId = Context.User?.FindFirst(ClaimTypes.NameIdentifier)?.Value
                                ?? Context.User?.FindFirst("sub")?.Value;
             var adminUserId = _configuration["Pulse:AdminUserId"]
                               ?? Environment.GetEnvironmentVariable("PULSE_ADMIN_USER_ID");
             return !string.IsNullOrEmpty(callerUserId) && callerUserId == adminUserId;
         }

    5. ADD OnConnectedAsync OVERRIDE — registers _userToConnection and pushes tokens:
         public override async Task OnConnectedAsync()
         {
             var userId = Context.User?.FindFirst(ClaimTypes.NameIdentifier)?.Value
                          ?? Context.User?.FindFirst("sub")?.Value ?? "";
             var displayName = Context.User?.FindFirst("displayName")?.Value ?? "Unknown";
             if (!string.IsNullOrEmpty(userId))
                 _userToConnection[userId] = Context.ConnectionId;
             var memberGroups = _whisperGroups.Values
                 .Where(g => g.MemberUserIds.Contains(userId))
                 .ToList();
             if (memberGroups.Count > 0)
             {
                 var tokens = memberGroups.Select(g => new {
                     groupId = g.GroupId,
                     groupName = g.Name,
                     liveKitToken = _liveKitService.GenerateRoomToken(g.LiveKitRoomName, userId, displayName),
                     liveKitHost = _liveKitService.GetLiveKitHost()
                 }).ToList();
                 await Clients.Caller.SendAsync("JoinWhisperGroups", tokens);
             }
             await base.OnConnectedAsync();
         }

    6. UPDATE OnDisconnectedAsync — at the start of the existing override, add:
         var userId = Context.User?.FindFirst(ClaimTypes.NameIdentifier)?.Value
                      ?? Context.User?.FindFirst("sub")?.Value ?? "";
         if (!string.IsNullOrEmpty(userId))
             _userToConnection.TryRemove(userId, out _);
       Place this BEFORE the existing foreach loop.

    7. ADD HUB METHODS — add these public async Task methods:

       CreateWhisperGroup(string groupId, string name, string visibility):
         - Guard: if (!IsServerAdmin()) return;
         - Validate visibility is one of "hidden", "existence", "full"; return if not.
         - Sanitize groupId: only allow alphanumeric and hyphens (reject if not matching ^[a-zA-Z0-9-]+$).
         - var liveKitRoomName = $"whisper-{groupId}";
         - var group = new WhisperGroup(groupId, name, visibility, new ConcurrentBag<string>(), liveKitRoomName);
         - if (!_whisperGroups.TryAdd(groupId, group)) return; // duplicate guard
         - await BroadcastWhisperGroupsAsync();

       AddWhisperMember(string groupId, string targetUserId):
         - Guard: if (!IsServerAdmin()) return;
         - if (!_whisperGroups.TryGetValue(groupId, out var group)) return;
         - if (group.MemberUserIds.Contains(targetUserId)) return; // idempotent
         - group.MemberUserIds.Add(targetUserId);
         - _userToWhisperGroups.GetOrAdd(targetUserId, _ => new()).Add(groupId);
         - await BroadcastWhisperGroupsAsync();
         - Push to online member if present:
             if (_userToConnection.TryGetValue(targetUserId, out var connId))
             {
                 var token = _liveKitService.GenerateRoomToken(group.LiveKitRoomName, targetUserId, targetUserId);
                 await Clients.Client(connId).SendAsync("WhisperGroupMemberAdded", new {
                     groupId, groupName = group.Name,
                     liveKitToken = token, liveKitHost = _liveKitService.GetLiveKitHost()
                 });
             }

       RemoveWhisperMember(string groupId, string targetUserId):
         - Guard: if (!IsServerAdmin()) return;
         - if (!_whisperGroups.TryGetValue(groupId, out var group)) return;
         - Rebuild ConcurrentBag without targetUserId (ConcurrentBag has no Remove; replace bag):
             var updated = group with { MemberUserIds = new ConcurrentBag<string>(group.MemberUserIds.Where(id => id != targetUserId)) };
             _whisperGroups[groupId] = updated;
         - Remove from _userToWhisperGroups similarly (rebuild bag for that userId).
         - await BroadcastWhisperGroupsAsync();
         - Push to online member if present:
             if (_userToConnection.TryGetValue(targetUserId, out var connId))
                 await Clients.Client(connId).SendAsync("WhisperGroupMemberRemoved", new { groupId });

       DissolveWhisperGroup(string groupId):
         - Guard: if (!IsServerAdmin()) return;
         - if (!_whisperGroups.TryRemove(groupId, out var group)) return;
         - For each memberId in group.MemberUserIds, remove from _userToWhisperGroups.
         - Push WhisperGroupDissolved to each online member:
             foreach (var memberId in group.MemberUserIds)
             {
                 if (_userToConnection.TryGetValue(memberId, out var connId))
                     await Clients.Client(connId).SendAsync("WhisperGroupDissolved", new { groupId });
             }
         - await BroadcastWhisperGroupsAsync();

    8. ADD BroadcastWhisperGroupsAsync() HELPER:
       Sends two payloads per-connection to respect visibility (per Pitfall 6 in RESEARCH.md):
         - For each connected client (iterate _userToConnection), determine visible groups:
             hidden: only visible to members
             existence: non-members see { groupId, name, memberCount, visibility }; members see full
             full: all clients see { groupId, name, members: [...], visibility }
         - Implementation pattern: foreach userId in _userToConnection.Keys, build their payload, Clients.Client(connId).SendAsync("WhisperGroupsUpdated", payload).
         - Member check: group.MemberUserIds.Contains(userId)
         - Payload shape per group entry for a given userId:
             Member view: { groupId, name, visibility, isMember=true, memberUserIds=[...] }
             Existence-visible non-member: { groupId, name, visibility="existence", isMember=false, memberCount=N }
             Hidden non-member: skip this group entirely
             Full non-member: { groupId, name, visibility="full", isMember=false, memberUserIds=[...] }

    IMPORTANT: Do not remove or alter any existing hub methods (JoinRoom, LeaveRoom, MuteChanged, DeafenChanged, AssignPrioritySpeaker, RemovePrioritySpeaker, BroadcastRoomListAsync, DeleteRoomAndBroadcast, GetRoomForConnection, GetRoomParticipants, BuildRoomListPayload).
    Do not touch LeaveRoom or OnDisconnectedAsync whisper-room cleanup — whisper rooms are server-scoped (D-01); leaving a voice room must NOT disconnect whisper (per Pitfall 5 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd /c/projetos/Hopin/Pulse.Server && dotnet build --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - dotnet build succeeds with 0 errors.
    - PresenceHub.cs contains CreateWhisperGroup, AddWhisperMember, RemoveWhisperMember, DissolveWhisperGroup, OnConnectedAsync, IsServerAdmin, BroadcastWhisperGroupsAsync.
    - _whisperGroups, _userToWhisperGroups, _userToConnection ConcurrentDictionary declarations present.
    - OnDisconnectedAsync clears _userToConnection at top of method.
    - Existing hub methods compile without modification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → SignalR Hub | All hub method calls arrive from authenticated JWT bearer; userId extracted from claims |
| Server → LiveKit | Token minted server-side; client never has access to API key/secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | CreateWhisperGroup / AddWhisperMember | mitigate | IsServerAdmin() guard on every whisper mutation; checks caller claim against PULSE_ADMIN_USER_ID config |
| T-04-02 | Spoofing | AddWhisperMember token generation | mitigate | Token generated server-side only after membership verified; client never requests token directly |
| T-04-03 | Tampering | groupId used in LiveKit room name | mitigate | Sanitize groupId to ^[a-zA-Z0-9-]+ before constructing whisper-{groupId} room name |
| T-04-04 | Information Disclosure | WhisperGroupsUpdated broadcast | mitigate | Per-recipient payload shaping in BroadcastWhisperGroupsAsync; hidden groups excluded from non-member payloads |
| T-04-05 | Tampering | _userToConnection staleness | mitigate | Overwrite on OnConnectedAsync; clear on OnDisconnectedAsync — last-write-wins prevents stale push |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. Run: cd /c/projetos/Hopin/Pulse.Server && dotnet build --no-restore
   Expected: Build succeeded, 0 Error(s)

2. Grep check — whisper methods present:
   grep -c "CreateWhisperGroup\|AddWhisperMember\|RemoveWhisperMember\|DissolveWhisperGroup\|OnConnectedAsync\|IsServerAdmin\|BroadcastWhisperGroupsAsync" Pulse.Server/Hubs/PresenceHub.cs
   Expected: count >= 7

3. Grep check — dictionaries present:
   grep -c "_whisperGroups\|_userToWhisperGroups\|_userToConnection" Pulse.Server/Hubs/PresenceHub.cs
   Expected: count >= 3

4. Grep check — existing hub methods NOT removed:
   grep -c "JoinRoom\|LeaveRoom\|MuteChanged\|AssignPrioritySpeaker" Pulse.Server/Hubs/PresenceHub.cs
   Expected: count >= 4
</verification>

<success_criteria>
- dotnet build succeeds with 0 errors
- PresenceHub contains all 4 whisper CRUD methods gated by IsServerAdmin()
- OnConnectedAsync pushes JoinWhisperGroups tokens to returning members
- OnDisconnectedAsync clears _userToConnection entry
- BroadcastWhisperGroupsAsync respects hidden/existence/full visibility per recipient
- No existing hub methods broken
</success_criteria>

<output>
Create .planning/phases/04-whisper-side-channel/04-01-SUMMARY.md when done
</output>
