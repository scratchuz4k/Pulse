---
phase: 02-full-room-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pulse.Server/Models/Room.cs
  - Pulse.Server/Data/AppDbContext.cs
  - Pulse.Server/Controllers/RoomsController.cs
  - Pulse.Server/Hubs/PresenceHub.cs
autonomous: true
requirements:
  - ROOM-03

must_haves:
  truths:
    - "POST /rooms with a JSON body { name } creates a room row in the DB and returns it"
    - "GET /rooms returns the full list of persisted rooms"
    - "After POST /rooms, all connected SignalR clients receive a RoomListUpdated event with the updated list"
    - "Duplicate room names return HTTP 409 (not a 500)"
  artifacts:
    - path: "Pulse.Server/Models/Room.cs"
      provides: "Room entity (Id, Name, CreatedAt)"
    - path: "Pulse.Server/Data/AppDbContext.cs"
      provides: "DbSet<Room> Rooms"
    - path: "Pulse.Server/Controllers/RoomsController.cs"
      provides: "GET /rooms + POST /rooms endpoints (replaces token-only controller)"
    - path: "Pulse.Server/Hubs/PresenceHub.cs"
      provides: "_connectionToRoom reverse-lookup populated in JoinRoom/LeaveRoom/OnDisconnected"
  key_links:
    - from: "RoomsController.CreateRoom"
      to: "IHubContext<PresenceHub>.Clients.All.SendAsync"
      via: "injected IHubContext<PresenceHub>"
      pattern: "hubContext\\.Clients\\.All\\.SendAsync.*RoomListUpdated"
    - from: "Pulse.Server/Data/AppDbContext.cs"
      to: "Pulse.Server/Models/Room.cs"
      via: "DbSet<Room>"
      pattern: "DbSet<Room>"
---

<objective>
Add server-side room persistence so rooms survive when empty.

Purpose: ROOM-03 requires rooms to persist in the DB (D-09). The existing RoomsController only issues LiveKit tokens — it needs GET /rooms and POST /rooms backed by EF Core. PresenceHub also needs a _connectionToRoom reverse-lookup that later plans rely on for MuteChanged.
Output: Room entity, AppDbContext extended, RoomsController with GET+POST, PresenceHub with reverse-lookup dict.
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projetos/Hopin/.planning/PROJECT.md
@C:/projetos/Hopin/.planning/ROADMAP.md

NOTE — Dev DB migration: EnsureCreated does NOT add new tables to an existing pulse.db.
Before testing, delete Pulse.Server/pulse.db so the schema recreates with the Room table.
Document this in a comment inside Program.cs or in a dev note at the top of this plan.
</context>

<interfaces>
From Pulse.Server/Data/AppDbContext.cs (current state):
  public DbSet<User> Users => Set<User>();
  public DbSet<RefreshToken> RefreshTokens => Set<RefreshToken>();

From Pulse.Server/Controllers/RoomsController.cs (current state):
  constructor: RoomsController(ILiveKitService liveKitService)
  only has: POST /rooms/token  (GetRoomToken — keep this, do NOT remove it)

From Pulse.Server/Hubs/PresenceHub.cs (current state):
  private record ParticipantInfo(string DisplayName, string UserId);
  private static readonly ConcurrentDictionary<string, ConcurrentDictionary<string, ParticipantInfo>> _rooms
  public async Task JoinRoom(string roomName)
  public async Task LeaveRoom(string roomName)
  public override async Task OnDisconnectedAsync(Exception? exception)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Room entity + AppDbContext extension</name>
  <files>Pulse.Server/Models/Room.cs, Pulse.Server/Data/AppDbContext.cs</files>
  <read_first>
    - Pulse.Server/Data/AppDbContext.cs (see current DbSets before adding)
    - Pulse.Server/Models/User.cs (follow existing model conventions)
  </read_first>
  <action>
    Create Pulse.Server/Models/Room.cs with:
      namespace Pulse.Server.Models;
      public class Room with properties: int Id (PK), string Name (non-null, default ""), DateTime CreatedAt (default DateTime.UtcNow).

    In AppDbContext.cs add: public DbSet<Room> Rooms => Set<Room>();

    In OnModelCreating (add the override if not present): add a unique index on Room.Name via modelBuilder.Entity<Room>().HasIndex(r => r.Name).IsUnique() so duplicate names fail at DB level.

    Dev note: After this change, delete pulse.db before running the server so EnsureCreatedAsync recreates the schema with the Room table. Add a comment in AppDbContext near the Rooms property: // Dev: delete pulse.db and restart to apply schema — EnsureCreated does not migrate existing DBs.
  </action>
  <verify>
    <automated>grep -r "DbSet&lt;Room&gt;" C:/projetos/Hopin/Pulse.Server/Data/AppDbContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - Pulse.Server/Models/Room.cs exists with Id (int), Name (string), CreatedAt (DateTime) properties in namespace Pulse.Server.Models
    - AppDbContext.cs contains "DbSet&lt;Room&gt; Rooms"
    - AppDbContext.cs contains a unique index configuration on Room.Name (HasIndex + IsUnique)
  </acceptance_criteria>
  <done>Room entity file exists; AppDbContext has Rooms DbSet with unique-name index configured.</done>
</task>

<task type="auto">
  <name>Task 2: RoomsController GET + POST + SignalR broadcast</name>
  <files>Pulse.Server/Controllers/RoomsController.cs</files>
  <read_first>
    - Pulse.Server/Controllers/RoomsController.cs (current file — keep the /token endpoint)
    - Pulse.Server/Controllers/AuthController.cs (follow constructor injection pattern)
    - Pulse.Server/Program.cs (confirm IHubContext is already registered via AddSignalR)
  </read_first>
  <action>
    Replace the constructor with: RoomsController(ILiveKitService liveKitService, AppDbContext db, IHubContext&lt;PresenceHub&gt; hubContext).
    Add required usings: Microsoft.EntityFrameworkCore, Pulse.Server.Data, Pulse.Server.Hubs, Microsoft.AspNetCore.SignalR.

    Add GET /rooms endpoint (no route suffix, so full path = GET /rooms):
      Query db.Rooms.OrderBy(r => r.Name).Select(r => new { r.Id, r.Name }).ToListAsync()
      Return Ok(rooms).

    Add POST /rooms endpoint (no route suffix, so full path = POST /rooms):
      Accept [FromBody] with a record or anonymous-compatible type containing string Name.
      Validate: if Name is null/whitespace return BadRequest({ error = "Name is required." }).
      Validate max length 80 chars; reject longer names with BadRequest.
      Try-insert: create new Room { Name = req.Name.Trim(), CreatedAt = DateTime.UtcNow }, add to db.Rooms, call SaveChangesAsync inside a try/catch for DbUpdateException.
      On DbUpdateException (unique constraint violation): return Conflict(new { error = "A room with that name already exists." }).
      On success: fetch updated list via db.Rooms.OrderBy(r => r.Name).Select(r => new { r.Id, r.Name }).ToListAsync(), then call hubContext.Clients.All.SendAsync("RoomListUpdated", rooms). Return Created("", new { room.Id, room.Name }).

    Keep the existing POST /rooms/token endpoint exactly as-is (it has route "token").
  </action>
  <verify>
    <automated>grep -c "RoomListUpdated" C:/projetos/Hopin/Pulse.Server/Controllers/RoomsController.cs</automated>
  </verify>
  <acceptance_criteria>
    - GET /rooms endpoint exists (method returns IActionResult, attribute [HttpGet] with no route argument)
    - POST /rooms endpoint exists (attribute [HttpPost] with no route argument)
    - POST /rooms/token endpoint still exists unchanged
    - RoomsController constructor accepts AppDbContext db and IHubContext&lt;PresenceHub&gt; hubContext
    - hubContext.Clients.All.SendAsync("RoomListUpdated", ...) call present in the POST handler
    - DbUpdateException catch block returns Conflict (HTTP 409)
    - Name validation: null/whitespace → BadRequest, length > 80 → BadRequest
  </acceptance_criteria>
  <done>GET /rooms and POST /rooms implemented; RoomListUpdated broadcast wired; existing token endpoint untouched.</done>
</task>

<task type="auto">
  <name>Task 3: PresenceHub _connectionToRoom reverse-lookup</name>
  <files>Pulse.Server/Hubs/PresenceHub.cs</files>
  <read_first>
    - Pulse.Server/Hubs/PresenceHub.cs (full file — understand existing _rooms dict and all methods)
  </read_first>
  <action>
    Add a second static dict alongside _rooms:
      private static readonly ConcurrentDictionary&lt;string, string&gt; _connectionToRoom = new();
      // maps connectionId -> roomName

    In JoinRoom: after _rooms.GetOrAdd(...).TryAdd(...), add _connectionToRoom[Context.ConnectionId] = roomName.

    In LeaveRoom: after room.TryRemove(...), add _connectionToRoom.TryRemove(Context.ConnectionId, out _).

    In OnDisconnectedAsync: after each room.TryRemove call, add _connectionToRoom.TryRemove(Context.ConnectionId, out _) once (outside the foreach, after the loop, since a connection is in exactly one room).

    Add a private helper: private static string? GetRoomForConnection(string connectionId) => _connectionToRoom.TryGetValue(connectionId, out var r) ? r : null;

    Also extend ParticipantInfo record to include IsMuted: private record ParticipantInfo(string DisplayName, string UserId, bool IsMuted = false);
    This is needed by Plan 02 (mute broadcast) and must land here to avoid a second edit of the same file.

    Do NOT add the MuteChanged hub method yet — that is Plan 02's task. Only add the record extension and the dict.
  </action>
  <verify>
    <automated>grep -c "_connectionToRoom" C:/projetos/Hopin/Pulse.Server/Hubs/PresenceHub.cs</automated>
  </verify>
  <acceptance_criteria>
    - _connectionToRoom ConcurrentDictionary&lt;string, string&gt; declared as static readonly field
    - _connectionToRoom[Context.ConnectionId] = roomName set inside JoinRoom
    - _connectionToRoom.TryRemove called in LeaveRoom
    - _connectionToRoom.TryRemove called in OnDisconnectedAsync
    - GetRoomForConnection(string connectionId) private static helper method exists
    - ParticipantInfo record now has three parameters: DisplayName, UserId, IsMuted (with default false)
  </acceptance_criteria>
  <done>PresenceHub has reverse-lookup dict populated on join/leave/disconnect; ParticipantInfo carries IsMuted field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → RoomsController | Authenticated (JWT [Authorize]) but room name is user-supplied |
| SignalR hub → _rooms/_connectionToRoom | In-memory state shared across hub instances (static dict) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02A-01 | Tampering | POST /rooms Name field | mitigate | Validate non-empty, max 80 chars, EF parameterization handles SQL injection |
| T-02A-02 | Denial of Service | POST /rooms unlimited creation | accept | No rate limiting in Phase 2 — dev-stage app, acceptable |
| T-02A-03 | Spoofing | _connectionToRoom integrity | mitigate | dict keyed by Context.ConnectionId (server-assigned, not client-supplied) |
</threat_model>

<verification>
Manual smoke test after deleting pulse.db:
1. Start server — confirm no startup errors.
2. GET /rooms with valid JWT → returns empty array [].
3. POST /rooms { "name": "general" } → returns 201, body has id and name fields.
4. GET /rooms → returns [{ id: 1, name: "general" }].
5. POST /rooms { "name": "general" } again → returns 409 with error message.
6. POST /rooms { "name": "" } → returns 400.
7. Client connected via SignalR: after step 3, confirm RoomListUpdated event fires in browser console.
</verification>

<success_criteria>
- Room table exists in pulse.db after server start
- GET /rooms returns persisted rooms as JSON array
- POST /rooms creates room + broadcasts RoomListUpdated
- Duplicate names return 409
- _connectionToRoom reverse-lookup is populated and cleared correctly (verified by code inspection)
</success_criteria>

<output>
Create .planning/phases/02-full-room-experience/02-01-SUMMARY.md when done
</output>
