---
plan: "04.1-04"
phase: "04.1-server-creation"
status: complete
completed: "2026-06-11"
---

## Summary

Replaced the DashBoard.vue placeholder with a real server landing page, and updated MainTemplate.vue's hardcoded "Ravengate" topbar with a live server switcher dropdown.

## What Was Built

- `pulse-client/src/renderer/views/DashBoard.vue` — Complete replacement:
  - `onMounted`: fetches `GET /servers/me` and populates `useServerStore.servers`
  - Empty state: "No servers yet" heading with Create and Join CTA buttons
  - Populated state: server card grid (name, invite code, Enter button)
  - Create Server modal: name input → `POST /servers` → navigates to `/server` on success
  - Join Server modal: invite code input → `POST /servers/join` → navigates to `/server` on success
  - Error display for 409 (name conflict) and invalid invite code
- `pulse-client/src/renderer/layouts/MainTemplate.vue` — Topbar server switcher:
  - Replaced hardcoded `>Ravengate<` with `{{ serverStore.activeServer?.name ?? '— select —' }}` button with chevron
  - Dropdown lists all user servers with active highlight; "Dashboard" link at bottom
  - Click-outside to close via `@click="switcherOpen = false"` on `app-root`
  - `switchToServer(id)` calls `serverStore.setActiveServer(id)` and navigates to `/server`

## Self-Check: PASSED

- TypeScript `npx tsc --noEmit`: 0 errors ✓
- DashBoard.vue does NOT contain "Coming soon", "Activity overview", `const stats = [` ✓
- DashBoard.vue contains `useServerStore`, `showCreateModal`, `showJoinModal`, `selectServer` ✓
- DashBoard.vue contains `fetch(\`${SERVER_URL}/servers/me\`)` ✓
- DashBoard.vue contains `fetch(\`${SERVER_URL}/servers\`)` for POST (createServer) ✓
- DashBoard.vue contains `fetch(\`${SERVER_URL}/servers/join\`)` for POST (joinServer) ✓
- MainTemplate.vue does NOT contain `>Ravengate<` ✓
- MainTemplate.vue contains `serverStore.activeServer?.name`, `switcherOpen`, `server-dropdown` ✓
