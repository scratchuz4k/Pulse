---
phase: 02-full-room-experience
plan: 09
type: execute
wave: 2
depends_on: [02-08]
files_modified:
  - pulse-client/src/renderer/composables/useLiveKit.ts
  - pulse-client/src/renderer/views/RoomView.vue
autonomous: true
gap_closure: true
requirements: [VOICE-05, VOICE-06, ROOM-02]
must_haves:
  truths:
    - "Joining a room while muted keeps the mic muted after join (others cannot hear you)"
    - "Joining a room while deafened keeps you deafened after join (you cannot hear and your mic stays muted)"
    - "The bottom voice bar no longer shows redundant speaking-ring indicators duplicating the squad panel"
  artifacts:
    - path: "pulse-client/src/renderer/composables/useLiveKit.ts"
      provides: "connect() respects desired mic state instead of hardcoding mic on"
    - path: "pulse-client/src/renderer/views/RoomView.vue"
      provides: "handleJoin re-applies isDeafened/mute state; bottom-bar speaking ring removed"
  key_links:
    - from: "pulse-client/src/renderer/views/RoomView.vue"
      to: "useLiveKit.connect"
      via: "post-connect state re-application"
      pattern: "isDeafened|setMicEnabled"
---

<objective>
Preserve mute/deafen state when joining a room, and remove the redundant speaking-ring indicators from the bottom voice bar.

Purpose: Closes UAT gaps — `useLiveKit.connect()` unconditionally enables the mic on every join, and `handleJoin()` only restores mute for PTT mode (ignores deafen and manual mute); separately the bottom bar speaking ring duplicates the squad panel and feels cluttered (test 6, minor).
Output: Join respects desired mic/deafen state; bottom-bar ring indicators removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-full-room-experience/02-05-SUMMARY.md

<interfaces>
useLiveKit, from pulse-client/src/renderer/composables/useLiveKit.ts:
- `connect(liveKitToken, liveKitHost)` — after `room.connect` + `room.startAudio`, unconditionally `await room.localParticipant.setMicrophoneEnabled(true)` and sets `isMicEnabled.value = true` (lines ~98-104).
- TrackSubscribed attaches an audio element `#livekit-audio-<identity>` to document.body and applies `setSinkId(activeOutputId)`. New elements default to `volume = 1`.
- Returns `{ connect, disconnect, toggleMic, isConnected, isMicEnabled, activeSpeakers, ... }`.

RoomView.vue:
- `isDeafened = ref(false)`, `prevMicEnabled = ref(false)`.
- `setMicEnabled(v)`: no-op if deafened; else toggles mic + broadcastMuteChanged.
- `handleJoin()`: createRoom → fetchLiveKitToken → connect (presence) → joinRoom → livekitConnect → `if (isPttMode.value) await setMicEnabled(false)`. Only PTT mode mutes on join; deafen + manual mute ignored.
- `handleToggleDeafen()`: on deafen sets all `audio[id^="livekit-audio-"]` elements `volume = 0`; on undeafen sets `volume = 1`.
- Bottom voice bar (`.voice-bar`, v-if isConnected): `.vb-avatars` renders `vb-av` with `:class="{ speaking: activeSpeakers.includes(p.userId), muted: p.isMuted }"`; `.vb-av.speaking` CSS adds `box-shadow: 0 0 0 2px var(--voice)` (the speaking ring). `.vb-speaking` text line shows "<name> speaking".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make connect() respect desired mic state</name>
  <files>pulse-client/src/renderer/composables/useLiveKit.ts</files>
  <action>
Change `connect(liveKitToken, liveKitHost)` to accept an optional `desiredMicEnabled: boolean = true` parameter. Replace the hardcoded `setMicrophoneEnabled(true)` block: call `await room.localParticipant.setMicrophoneEnabled(desiredMicEnabled)` and set `isMicEnabled.value = desiredMicEnabled`. Keep the try/catch and logging. Do not change device-refresh logic.
  </action>
  <verify>
    <automated>cd pulse-client && npx vue-tsc --noEmit</automated>
  </verify>
  <done>connect() enables the mic only when desiredMicEnabled is true; defaults preserve current behavior for callers that omit it.</done>
</task>

<task type="auto">
  <name>Task 2: Re-apply mute/deafen on join in RoomView</name>
  <files>pulse-client/src/renderer/views/RoomView.vue</files>
  <action>
In `handleJoin()`, compute the desired mic state before calling LiveKit connect: the mic should be OFF if `isPttMode.value` OR `isDeafened.value` OR the user had manually muted (track current mute intent — since mute state pre-join is just `isMicEnabled` being false from a prior session, treat any non-default-on case). Pass it through: `await livekitConnect(liveKitToken, liveKitHost, desiredMic)` where `desiredMic = !isPttMode.value && !isDeafened.value`.

After connect, if `isDeafened.value` is true: set all existing remote audio elements `volume = 0` (reuse the selector `audio[id^="livekit-audio-"]`) so freshly-attached tracks are silenced, and ensure mic stays muted (it already is, since desiredMic was false). Also broadcast the correct mute state to peers via `broadcastMuteChanged(true)` when joining muted/deafened so the squad panel shows your 🔇 to others.

Remove the now-redundant trailing `if (isPttMode.value) await setMicEnabled(false)` line (its effect is now handled by desiredMic).

Note: TrackSubscribed attaches new audio elements at volume 1 during the session; if deafened mid-session this is already handled by handleToggleDeafen. For deafened-at-join, the post-connect volume-zero pass covers tracks subscribed during connect; rely on existing deafen handling for later joins.
  </action>
  <verify>
    <automated>cd pulse-client && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Joining while muted keeps mic muted; joining while deafened keeps audio silenced and mic muted; peers see correct mute icon.</done>
</task>

<task type="auto">
  <name>Task 3: Remove redundant bottom-bar speaking ring</name>
  <files>pulse-client/src/renderer/views/RoomView.vue</files>
  <action>
The squad panel already shows speaking state (animated ring + status text), so the bottom-bar avatar speaking ring is redundant clutter (UAT test 6). Remove the `speaking` class binding from the `.vb-av` avatars in the bottom voice bar — change `:class="{ speaking: activeSpeakers.includes(p.userId), muted: p.isMuted }"` to `:class="{ muted: p.isMuted }"`. Remove the now-unused `.vb-av.speaking` CSS rule.

Keep the `.vb-speaking` text line ("<name> speaking" / "Voice connected") — that is informational, not a redundant ring. Do not remove the squad-panel ring.
  </action>
  <verify>
    <automated>cd pulse-client && npx vue-tsc --noEmit</automated>
  </verify>
  <done>Bottom-bar avatars no longer render the speaking ring; squad-panel speaking indicator unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → LiveKit | Local mic publish state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02J-01 | Information Disclosure | unintended mic-on at join leaking audio | mitigate | connect() now publishes mic only when desiredMic is true; deafen/mute states force it off at join |
</threat_model>

<verification>
- Mute mic, leave, rejoin → still muted (peer sees 🔇). Deafen, leave, rejoin → still deafened (no audio heard, mic muted). Bottom bar shows no ring around speaking avatars while the squad panel still does.
</verification>

<success_criteria>
Join preserves mute/deafen state and the bottom-bar speaking ring is gone.
</success_criteria>

<output>
Create `.planning/phases/02-full-room-experience/02-09-SUMMARY.md` when done
</output>
