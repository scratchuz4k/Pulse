---
phase: 02-full-room-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pulse-client/src/renderer/stores/room.ts
  - pulse-client/src/renderer/composables/usePresence.ts
  - pulse-client/src/renderer/views/RoomView.vue
autonomous: true
requirements:
  - ROOM-03

must_haves:
  truths:
    - "Voice tab shows a card for every room returned by GET /rooms, not just the one the user is in"
    - "Clicking a room card in the voice feed joins that room (same as typing the name and pressing Jump in)"
    - "New Room button / form POSTs to /rooms and the new room appears in every connected client's voice feed without a page refresh"
    - "RoomListUpdated SignalR event from the server updates roomStore.rooms in real time"
  artifacts:
    - path: "pulse-client/src/renderer/stores/room.ts"
      provides: "rooms: RoomInfo[] state + setRoomList action"
    - path: "pulse-client/src/renderer/composables/usePresence.ts"
      provides: "RoomListUpdated listener + createRoom exported function"
    - path: "pulse-client/src/renderer/views/RoomView.vue"
      provides: "Voice tab renders roomStore.rooms; New room button POSTs via createRoom; room cards are clickable"
  key_links:
    - from: "RoomView.vue New room form submit"
      to: "usePresence.createRoom"
      via: "direct import + await"
      pattern: "createRoom"
    - from: "usePresence RoomListUpdated listener"
      to: "roomStore.setRoomList"
      via: "hubConnection.on callback"
      pattern: "setRoomList"
    - from: "usePresence connect()"
      to: "GET /rooms"
      via: "fetch on SignalR connect"
      pattern: "fetch.*rooms"
---

<objective>
Wire the voice feed to real persistent room data from the server.

Purpose: The Voice tab currently shows only the room the user has joined. After Plan 01 delivers GET /rooms and POST /rooms, this plan wires those endpoints into the client so the voice feed lists all rooms and allows one-click join and room creation.
Output: roomStore.rooms[]; usePresence fetches on connect + listens for RoomListUpdated; voice feed renders all rooms; New room button creates via POST.
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projetos/Hopin/.planning/PROJECT.md
@C:/projetos/Hopin/.planning/ROADMAP.md
@C:/projetos/Hopin/.planning/phases/02-full-room-experience/02-01-SUMMARY.md
</context>

<interfaces>
From pulse-client/src/renderer/stores/room.ts (after Plan 02 additions):
  export interface Participant { connectionId, displayName, userId, isMuted: boolean }
  exported: setRoom, addParticipant, removeParticipant, clearRoom, setParticipantMuted

From pulse-client/src/renderer/composables/usePresence.ts (after Plan 02 additions):
  exported: connect, joinRoom, leaveRoom, disconnect, connectionState, broadcastMuteChanged

From pulse-client/src/renderer/views/RoomView.vue (current):
  Voice tab has: feed-topbar with "New room" button, showJoinForm toggle, join-card with room-input + handleJoin
  handleJoin: calls fetchLiveKitToken, connect(SERVER_URL), joinRoom, livekitConnect
  SERVER_URL = import.meta.env.VITE_SERVER_URL || 'http://localhost:5000'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: roomStore rooms list + usePresence RoomListUpdated wiring</name>
  <files>
    pulse-client/src/renderer/stores/room.ts,
    pulse-client/src/renderer/composables/usePresence.ts
  </files>
  <read_first>
    - pulse-client/src/renderer/stores/room.ts (full file — see current state after Plan 02 changes)
    - pulse-client/src/renderer/composables/usePresence.ts (full file — see connect() and return object)
  </read_first>
  <action>
    In stores/room.ts:
      1. Add interface: export interface RoomInfo { id: number; name: string }
      2. Add reactive state: const rooms = ref&lt;RoomInfo[]&gt;([])
      3. Add action: function setRoomList(list: RoomInfo[]): void { rooms.value = list }
      4. Export rooms and setRoomList in the store return object.

    In usePresence.ts:
      1. Add a RoomListUpdated listener inside connect(), after the existing .on() calls:
           hubConnection.on('RoomListUpdated', (list: { id: number; name: string }[]) => {
             roomStore.setRoomList(list)
           })
      2. After hubConnection.start() succeeds (inside the try block, after connectionState is set to 'connected'), fetch the initial room list:
           const serverUrl comes from the connect() parameter. Add a fetch call:
             fetch(`${serverUrl}/rooms`, {
               headers: { Authorization: `Bearer ${authStore.accessToken}` }
             })
               .then(r => r.json())
               .then((list: { id: number; name: string }[]) => roomStore.setRoomList(list))
               .catch(e => console.error('[usePresence] failed to load rooms:', e))
      3. Add exported async function createRoom(serverUrl: string, name: string): Promise&lt;void&gt;:
           fetch POST to `${serverUrl}/rooms` with Content-Type: application/json and Authorization header from authStore.accessToken.
           Body: JSON.stringify({ name }).
           If response is not ok and status is 409, throw new Error('A room with that name already exists.').
           If response is not ok otherwise, throw new Error('Failed to create room.').
           On success: the server broadcasts RoomListUpdated so no manual store update needed here.
      4. Export createRoom in the return object of usePresence().
  </action>
  <verify>
    <automated>grep -c "setRoomList" C:/projetos/Hopin/pulse-client/src/renderer/stores/room.ts</automated>
  </verify>
  <acceptance_criteria>
    - stores/room.ts exports interface RoomInfo { id: number; name: string }
    - stores/room.ts has const rooms = ref&lt;RoomInfo[]&gt;([]) and function setRoomList exported
    - usePresence.ts has hubConnection.on('RoomListUpdated', ...) listener inside connect()
    - usePresence.ts fetches GET /rooms after successful hub connection and calls roomStore.setRoomList
    - usePresence.ts exports createRoom(serverUrl, name) that POSTs to /rooms and throws descriptive errors on failure
  </acceptance_criteria>
  <done>rooms[] in store; RoomListUpdated listener active; createRoom exported from usePresence.</done>
</task>

<task type="auto">
  <name>Task 2: Voice feed renders all rooms + New room creates via POST</name>
  <files>pulse-client/src/renderer/views/RoomView.vue</files>
  <read_first>
    - pulse-client/src/renderer/views/RoomView.vue (full file — voice-view section + script setup)
  </read_first>
  <action>
    In the script setup section:
      1. Import createRoom from usePresence alongside existing destructured exports.
      2. Add ref: const createRoomError = ref('').
      3. Change handleJoin to also create the room via POST before joining:
           async function handleJoin(): Promise&lt;void&gt; {
             if (!roomNameInput.value.trim()) return
             joinError.value = ''; createRoomError.value = ''
             joining.value = true; showJoinForm.value = false
             try {
               // Create the room on the server (idempotent — server returns existing if name taken by POST design)
               await createRoom(SERVER_URL, roomNameInput.value.trim())
             } catch (e) {
               // Non-fatal: room may already exist, proceed to join
               console.warn('[handleJoin] createRoom:', e)
             }
             try {
               const { liveKitToken, liveKitHost } = await fetchLiveKitToken(roomNameInput.value.trim())
               await connect(SERVER_URL)
               await joinRoom(roomNameInput.value.trim())
               await livekitConnect(liveKitToken, liveKitHost)
               roomNameInput.value = ''
               activeNav.value = 'voice'
             } catch (err: unknown) {
               joinError.value = err instanceof Error ? err.message : 'Failed to join'
               showJoinForm.value = true
             } finally {
               joining.value = false
             }
           }
      4. Add a handleJoinRoom(name: string) helper for one-click card joins:
           async function handleJoinRoom(name: string): Promise&lt;void&gt; {
             roomNameInput.value = name
             await handleJoin()
           }

    In the Voice tab template (inside .voice-view .feed-col .feed-scroll):
      1. Replace or augment the existing static room card section. Keep the active-card showing the current room.
      2. After the active-card (or instead of the empty-card when no rooms exist), render the full room list from roomStore.rooms using v-for:
           For each room in roomStore.rooms where room.name !== roomStore.currentRoomName (i.e., rooms the user is NOT in), render a .room-card that is clickable and calls handleJoinRoom(room.name).
           Card content: room name in .card-name, a "Join ▸" button with class jump-btn.
           If roomStore.rooms is empty and user is not in a room, show the existing empty-card with "No voice rooms yet." message.
      3. Add CSS: .room-card.joinable { cursor: pointer; } and .room-card.joinable:hover { border-color: var(--accent); } in the scoped style block.

    NOTE: Keep the existing join-card form (shown when showJoinForm is true) for creating/joining new rooms by typing a name. The room list cards are for joining existing rooms. The "New room" button in the topbar continues to toggle showJoinForm.
  </action>
  <verify>
    <automated>grep -c "roomStore.rooms" C:/projetos/Hopin/pulse-client/src/renderer/views/RoomView.vue</automated>
  </verify>
  <acceptance_criteria>
    - createRoom is imported from usePresence() in script setup
    - handleJoin calls createRoom(SERVER_URL, name) before joining (errors are caught and non-fatal)
    - handleJoinRoom(name) helper exists and sets roomNameInput + calls handleJoin
    - Template has v-for loop over roomStore.rooms rendering a .room-card per room
    - Cards for rooms the user is NOT in have a clickable Join button that calls handleJoinRoom
    - .room-card.joinable CSS hover rule exists in scoped styles
  </acceptance_criteria>
  <done>Voice feed lists all server rooms; clicking a card joins it; new room form POSTs to /rooms first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → GET /rooms | Authenticated fetch with Bearer token |
| Renderer → POST /rooms | Authenticated fetch; name is user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02C-01 | Tampering | Room name in POST body | mitigate | Server validates non-empty + max 80 chars (Plan 01 task 2); client trims input |
| T-02C-02 | Info Disclosure | GET /rooms returns all room names | accept | All authenticated users can see all rooms — D-08 says any user can create; listing is equivalent |
</threat_model>

<verification>
Manual smoke test:
1. Server running with fresh pulse.db. Client A connects.
2. Voice tab: shows empty state or existing rooms from DB.
3. Client A creates room "standup" via the New room form. Voice feed shows "standup" card.
4. Client B connects — Voice tab immediately shows "standup" room card (via GET /rooms on connect).
5. Client B clicks the "standup" card → joins the room. Client A sees Client B appear in the participant list.
6. Create another room "afterparty" from Client B → both clients' voice feeds update live via RoomListUpdated.
</verification>

<success_criteria>
- Voice feed lists all rooms from GET /rooms on connect
- RoomListUpdated event updates feed in real time across clients
- One-click join from room card works
- Room creation via form POSTs to server before joining
</success_criteria>

<output>
Create .planning/phases/02-full-room-experience/02-03-SUMMARY.md when done
</output>
