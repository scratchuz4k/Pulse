---
phase: "04-whisper-side-channel"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pulse-client/src/renderer/composables/useLiveKit.ts
autonomous: true
requirements:
  - WHISP-02
  - WHISP-03

must_haves:
  truths:
    - "Main room connects and disconnects without touching whisper rooms"
    - "connectWhisper(groupId, token, host) creates a parallel Room and attaches audio with id prefix whisper-audio-{groupId}-{identity}"
    - "disconnectWhisper(groupId) removes only that whisper room, main room unaffected"
    - "applyDucking() does NOT reduce volume on whisper audio elements"
    - "switchOutput() applies the selected sink to both main and whisper audio elements"
    - "whisperActiveSpeakers Map is updated from each whisper room's ActiveSpeakersChanged event"
  artifacts:
    - path: "pulse-client/src/renderer/composables/useLiveKit.ts"
      provides: "Multi-room LiveKit management"
      contains: "connectWhisper"
  key_links:
    - from: "connectWhisper"
      to: "whisperRooms Map"
      via: "whisperRooms.set(groupId, room)"
      pattern: "whisperRooms.set"
    - from: "applyDucking"
      to: "audio element selector"
      via: "querySelectorAll"
      pattern: "livekit-audio-"
---

<objective>
Refactor useLiveKit.ts to support N parallel LiveKit rooms: rename the module-level singleton to mainRoom, add a whisperRooms Map, and introduce connectWhisper/disconnectWhisper functions. Fix applyDucking() and switchOutput() to be whisper-safe. Expose whisperActiveSpeakers for the store to consume.

Purpose: Enables simultaneous main room + whisper audio — the core WHISP-02/WHISP-03 capability. Without this refactor, any whisper connect call would destroy the main voice room.
Output: Modified useLiveKit.ts with Map-based multi-room management and safe ducking/output selectors.
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-whisper-side-channel/04-CONTEXT.md
@.planning/phases/04-whisper-side-channel/04-RESEARCH.md

<interfaces>
Current useLiveKit.ts module-level variables (lines 9-17, VERIFIED):
  let livekitRoom: Room | null = null   ← rename to mainRoom
  const isConnected = ref(false)
  const isMicEnabled = ref(false)
  const activeSpeakers = ref<string[]>([])
  const inputDevices, outputDevices, activeInputId, activeOutputId
  const prioritySpeakerId = ref<string | null>(null)

Functions in current return object (lines 169-174):
  connect, disconnect, toggleMic, switchInput, switchOutput,
  isConnected, isMicEnabled, activeSpeakers,
  inputDevices, outputDevices, activeInputId, activeOutputId,
  prioritySpeakerId, setPrioritySpeaker

Call sites that reference livekitRoom (ALL must be updated):
  - connect() lines 72-75: disconnects livekitRoom before creating new
  - switchInput() line 138: livekitRoom.switchActiveDevice
  - switchOutput() lines 144,149: queries audio[id^="livekit-audio-"] + livekitRoom.switchActiveDevice
  - disconnect() line 153: livekitRoom.disconnect() + null
  - toggleMic() line 163: livekitRoom.localParticipant.setMicrophoneEnabled

RoomView.vue calls useLiveKit as (lines 178-180):
  const { connect: livekitConnect, isConnected, isMicEnabled, activeSpeakers } = useLiveKit()
  ← connect() alias must stay named "connect" in the returned object (or alias is fine)
  ← isConnected, isMicEnabled, activeSpeakers must remain in return object with same names

Audio element ID conventions (CRITICAL per RESEARCH.md Pitfall 2):
  Main room:    id = "livekit-audio-{identity}"          ← existing, keep
  Whisper room: id = "whisper-audio-{groupId}-{identity}" ← new prefix, never "livekit-audio-"

applyDucking() current selector (line 53): audio[id^="livekit-audio-"]
  ← This is CORRECT for ducking; whisper audio uses different prefix so ducking is safe after refactor
  ← No change needed to applyDucking selector itself; just ensure whisper attach uses correct id prefix

switchOutput() current selector (line 144): audio[id^="livekit-audio-"]
  ← MUST be extended to also match audio[id^="whisper-audio-"] (Pitfall 3 in RESEARCH.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename livekitRoom to mainRoom and add whisperRooms Map</name>
  <files>pulse-client/src/renderer/composables/useLiveKit.ts</files>
  <read_first>
    - pulse-client/src/renderer/composables/useLiveKit.ts (full file — read every line before editing)
    - .planning/phases/04-whisper-side-channel/04-RESEARCH.md (Critical Code Analysis §1, Pitfalls 1, 2, 3)
  </read_first>
  <action>
    Make the following targeted changes to useLiveKit.ts:

    1. RENAME module-level variable: replace
         let livekitRoom: Room | null = null
       with:
         let mainRoom: Room | null = null
         const whisperRooms = new Map<string, Room>()

    2. ADD whisperActiveSpeakers module-level ref:
         const whisperActiveSpeakers = ref<Map<string, string[]>>(new Map())
       Place it alongside the other module-level refs (after activeSpeakers).

    3. UPDATE all 4 references inside useLiveKit() function body that used livekitRoom:
       a. In connect() — rename: livekitRoom → mainRoom throughout (lines 72-117 approx).
          The Room created in connect() is the main room; rename the local variable too if needed.
       b. In switchInput() — rename: livekitRoom → mainRoom.
       c. In switchOutput():
          - Rename livekitRoom → mainRoom.
          - Extend the querySelectorAll selector from
              'audio[id^="livekit-audio-"]'
            to select BOTH main and whisper elements:
              document.querySelectorAll<HTMLAudioElement>('audio[id^="livekit-audio-"], audio[id^="whisper-audio-"]')
       d. In disconnect() — rename: livekitRoom → mainRoom. This function disconnects ONLY the main room; do not touch whisperRooms here.
       e. In toggleMic() — rename: livekitRoom → mainRoom.

    4. ADD connectWhisper function inside useLiveKit():
         async function connectWhisper(groupId: string, token: string, host: string): Promise<void> {
           if (whisperRooms.has(groupId)) return  // already connected — idempotent
           const room = new Room({ adaptiveStream: true, dynacast: true })

           room.on(RoomEvent.TrackSubscribed, (track, _publication, participant) => {
             if (track.kind === Track.Kind.Audio) {
               const el = track.attach()
               el.id = `whisper-audio-${groupId}-${participant.identity}`   // CRITICAL: distinct prefix
               if (activeOutputId.value && typeof (el as HTMLAudioElement).setSinkId === 'function') {
                 (el as HTMLAudioElement).setSinkId(activeOutputId.value).catch(() => {})
               }
               document.body.appendChild(el)
             }
           })

           room.on(RoomEvent.TrackUnsubscribed, (track, _pub, participant) => {
             track.detach().forEach(el => el.remove())
             document.getElementById(`whisper-audio-${groupId}-${participant.identity}`)?.remove()
           })

           room.on(RoomEvent.ParticipantDisconnected, (participant) => {
             document.getElementById(`whisper-audio-${groupId}-${participant.identity}`)?.remove()
           })

           room.on(RoomEvent.ActiveSpeakersChanged, (speakers) => {
             const identities = speakers.map(s => s.identity)
             const updated = new Map(whisperActiveSpeakers.value)
             updated.set(groupId, identities)
             whisperActiveSpeakers.value = updated
           })

           room.on(RoomEvent.Disconnected, () => {
             whisperRooms.delete(groupId)
             const updated = new Map(whisperActiveSpeakers.value)
             updated.delete(groupId)
             whisperActiveSpeakers.value = updated
             // Remove all audio elements for this group
             document.querySelectorAll<HTMLAudioElement>(`audio[id^="whisper-audio-${groupId}-"]`).forEach(el => el.remove())
           })

           await room.connect(host, token)
           await room.startAudio()
           whisperRooms.set(groupId, room)
           console.log(`[LiveKit] whisper room connected: ${groupId}`)
         }

    5. ADD disconnectWhisper function inside useLiveKit():
         async function disconnectWhisper(groupId: string): Promise<void> {
           const room = whisperRooms.get(groupId)
           if (!room) return
           await room.disconnect()
           whisperRooms.delete(groupId)
           const updated = new Map(whisperActiveSpeakers.value)
           updated.delete(groupId)
           whisperActiveSpeakers.value = updated
           document.querySelectorAll<HTMLAudioElement>(`audio[id^="whisper-audio-${groupId}-"]`).forEach(el => el.remove())
         }

    6. ADD getWhisperRoom helper (used by transmit mode to publish mic to whisper room):
         function getWhisperRoom(groupId: string): Room | undefined {
           return whisperRooms.get(groupId)
         }

    7. UPDATE return object — add new exports alongside existing ones:
         return {
           connect, disconnect, toggleMic, switchInput, switchOutput,
           isConnected, isMicEnabled, activeSpeakers,
           inputDevices, outputDevices, activeInputId, activeOutputId,
           prioritySpeakerId, setPrioritySpeaker,
           connectWhisper, disconnectWhisper, getWhisperRoom,
           whisperActiveSpeakers,
         }

    DO NOT change applyDucking() selector — it correctly targets only "livekit-audio-" prefix.
    DO NOT change the connect() function signature — RoomView.vue calls it as livekitConnect(token, host, desiredMic).
    DO NOT call uIOhook.start() or import any Electron modules — this is renderer code only.
  </action>
  <verify>
    <automated>cd /c/projetos/Hopin/pulse-client && npm run typecheck 2>&1 | grep -i error | head -20</automated>
  </verify>
  <done>
    - npm run typecheck (or tsc --noEmit) reports 0 errors in useLiveKit.ts.
    - File contains: mainRoom, whisperRooms Map, connectWhisper, disconnectWhisper, getWhisperRoom, whisperActiveSpeakers.
    - livekitRoom no longer appears anywhere in useLiveKit.ts.
    - switchOutput selector includes both "livekit-audio-" and "whisper-audio-" prefixes.
    - whisper audio attach uses id prefix "whisper-audio-{groupId}-{identity}".
    - connect() and disconnect() affect only mainRoom.
    - Return object exports connectWhisper, disconnectWhisper, getWhisperRoom, whisperActiveSpeakers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → LiveKit SDK | Token received via SignalR push (server-minted); renderer passes token to Room.connect() |
| DOM audio elements | Audio elements attached to document.body; prefix isolation prevents ducking selector collision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | whisper audio element id collision | mitigate | Audio elements use "whisper-audio-{groupId}-{identity}" prefix; applyDucking selector "livekit-audio-" cannot match |
| T-04-07 | Denial of Service | connect() destroying main room on whisper call | mitigate | connectWhisper is a separate function; existing connect() renamed internally to use mainRoom; paths are independent |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — livekit-client already installed |
</threat_model>

<verification>
1. TypeScript check:
   cd /c/projetos/Hopin/pulse-client && npm run typecheck 2>&1 | tail -10
   Expected: 0 errors

2. Grep — mainRoom present, livekitRoom absent:
   grep -c "mainRoom" pulse-client/src/renderer/composables/useLiveKit.ts
   grep -c "livekitRoom" pulse-client/src/renderer/composables/useLiveKit.ts
   Expected: mainRoom >= 5, livekitRoom == 0

3. Grep — whisper audio prefix in connectWhisper attach:
   grep "whisper-audio-" pulse-client/src/renderer/composables/useLiveKit.ts
   Expected: at least 1 match with whisper-audio-${groupId}-${participant.identity}

4. Grep — switchOutput extended selector:
   grep "whisper-audio-" pulse-client/src/renderer/composables/useLiveKit.ts | grep "setSinkId\|switchOutput\|querySelectorAll"
   Expected: switchOutput queries both prefixes

5. Grep — connectWhisper exported:
   grep "connectWhisper" pulse-client/src/renderer/composables/useLiveKit.ts
   Expected: >= 3 occurrences (function def + return object + type usage)
</verification>

<success_criteria>
- TypeScript compiles with 0 errors
- livekitRoom is fully replaced by mainRoom throughout the file
- whisperRooms Map manages N parallel whisper Room instances
- connectWhisper attaches audio with "whisper-audio-{groupId}-{identity}" id prefix
- disconnectWhisper removes room and cleans up audio elements
- switchOutput applies sink to both main and whisper audio elements
- applyDucking remains unchanged and cannot affect whisper audio (prefix safe)
- All existing public API (connect, disconnect, toggleMic, etc.) continues to work
</success_criteria>

<output>
Create .planning/phases/04-whisper-side-channel/04-02-SUMMARY.md when done
</output>
