---
phase: 02-full-room-experience
plan: 07
type: execute
wave: 2
depends_on: [02-06]
files_modified:
  - pulse-client/src/main/index.ts
  - pulse-client/src/preload/index.ts
  - pulse-client/src/renderer/composables/usePtt.ts
autonomous: true
gap_closure: true
requirements: [VOICE-04]
must_haves:
  truths:
    - "After choosing Push-to-Talk mode and restarting the app, the app is still in Push-to-Talk mode (not reset to Voice Activity)"
    - "The PTT key binding continues to persist across restarts (existing behavior preserved)"
  artifacts:
    - path: "pulse-client/src/main/index.ts"
      provides: "ptt.mode persisted in electron-store with get/set IPC handlers"
      contains: "ptt.mode"
    - path: "pulse-client/src/renderer/composables/usePtt.ts"
      provides: "isPttMode loaded from store on mount, saved on change"
  key_links:
    - from: "pulse-client/src/renderer/composables/usePtt.ts"
      to: "electron-store ptt.mode"
      via: "getPttMode on mount, setPttMode on watch"
      pattern: "getPttMode|setPttMode"
---

<objective>
Persist the Voice-Activity vs Push-to-Talk mode selection across restarts.

Purpose: Closes UAT gap (test 2) — only `ptt.key` is saved; `isPttMode` is a plain `ref(false)` reset on every mount, so the app always boots into Voice Activity.
Output: `ptt.mode` stored in electron-store with IPC get/set; usePtt restores and watches it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-full-room-experience/02-04-SUMMARY.md

<interfaces>
electron-store + PTT IPC, from pulse-client/src/main/index.ts:
- `store` (electron-store, encryptionKey = machineId)
- IPC `ptt:get-key` → `store.get('ptt.key') ?? null`
- IPC `ptt:set-key` → validates and `store.set('ptt.key', accelerator)`
- After plan 06, this file also manages `currentPttKeycode` and uiohook.

Preload bridge, from pulse-client/src/preload/index.ts:
- Exposes `getPttKey`, `setPttKey`, `onPttKeyDown`, `onPttKeyUp`, `removePttListeners`, `storeGet/Set/Del`.

usePtt, from pulse-client/src/renderer/composables/usePtt.ts (after plan 06):
- `const isPttMode = ref(false)`
- `onMounted` loads `getPttKey()` into `pttBinding`.
- Returns `{ isPttMode, pttBinding, isCapturing, startCapture, handleCaptureKeydown }`.

RoomView.vue Settings toggle binds `isPttMode` directly (`@click="isPttMode = false/true"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ptt.mode store + IPC handlers (main + preload)</name>
  <files>pulse-client/src/main/index.ts, pulse-client/src/preload/index.ts</files>
  <action>
In main/index.ts, add two IPC handlers alongside the existing `ptt:get-key` / `ptt:set-key`:
- `ipcMain.handle('ptt:get-mode', () => (store.get('ptt.mode') as boolean | undefined) ?? false)` — returns whether PTT mode is enabled, default false (Voice Activity).
- `ipcMain.handle('ptt:set-mode', (_event, enabled: boolean) => { store.set('ptt.mode', !!enabled) })`.

In preload/index.ts, expose on `pulseApi`:
- `getPttMode: () => ipcRenderer.invoke('ptt:get-mode') as Promise<boolean>`
- `setPttMode: (enabled: boolean) => ipcRenderer.invoke('ptt:set-mode', enabled)`

If a shared `window.pulseApi` TypeScript type/declaration exists (search for `pulseApi` in a `.d.ts`), add the two methods there so the renderer type-checks. If no declaration file exists, leave as-is (the renderer accesses via `window.pulseApi`).
  </action>
  <verify>
    <automated>cd pulse-client && npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <done>ptt:get-mode / ptt:set-mode handlers exist in main, getPttMode/setPttMode exposed in preload, type-checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restore and persist isPttMode in usePtt</name>
  <files>pulse-client/src/renderer/composables/usePtt.ts</files>
  <action>
In usePtt:
- In `onMounted`, after loading the saved key, call `const savedMode = await window.pulseApi.getPttMode()` and set `isPttMode.value = savedMode`.
- Add a `watch(isPttMode, (v) => { window.pulseApi.setPttMode(v) })` so toggling the mode in Settings persists immediately. Import `watch` from 'vue'.

Guard against the watch firing during the initial restore overwriting the store with the same value (harmless, but to be safe set `isPttMode.value` before registering the watch, OR register the watch and accept the idempotent write — either is acceptable; prefer setting value first then watching).
  </action>
  <verify>
    <automated>cd pulse-client && npx vue-tsc --noEmit</automated>
  </verify>
  <done>usePtt restores isPttMode from store on mount and writes ptt.mode on change; type-checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main IPC | Renderer sets a persisted preference flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02H-01 | Tampering | ptt:set-mode payload | mitigate | Coerce to boolean (`!!enabled`) before store.set; non-boolean inputs cannot corrupt the flag |
</threat_model>

<verification>
- Switch to Push-to-Talk in Settings, fully close and reopen the app — Settings shows Push-to-Talk selected and PTT works without re-toggling.
</verification>

<success_criteria>
Voice-Activity / Push-to-Talk selection survives an app restart.
</success_criteria>

<output>
Create `.planning/phases/02-full-room-experience/02-07-SUMMARY.md` when done
</output>
