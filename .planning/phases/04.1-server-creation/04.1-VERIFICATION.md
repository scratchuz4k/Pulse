---
phase: "04.1-server-creation"
status: passed
verified: "2026-06-11"
requirements: [SRV-10, SRV-11, SRV-12, SRV-13]
---

# Phase 4.1: Server Creation — Verification

## Phase Goal

Users can create named servers (organizations) that group voice rooms under one roof. Each server has an owner/admin; members join via invite code.

## Must-Have Verification

### SRV-10 — Server creation and ownership

| Truth | Status | Evidence |
|-------|--------|----------|
| Server entity exists with Id, Name, OwnerId (Guid), InviteCode, CreatedAt | ✅ | `Pulse.Server/Models/Server.cs` |
| POST /servers creates a server; caller becomes OwnerId; response includes inviteCode | ✅ | `ServersController.CreateServer` — sets `OwnerId = ownerGuid`, returns `{ Id, Name, OwnerId, InviteCode }` |
| DashBoard.vue Create Server modal posts to /servers and navigates to /server | ✅ | `DashBoard.vue createServer()` |
| MainTemplate.vue topbar shows active server name from useServerStore | ✅ | `{{ serverStore.activeServer?.name ?? '— select —' }}` |
| Server switcher dropdown lists all user servers and sets active server on click | ✅ | `MainTemplate.vue switchToServer(id)` |

### SRV-11 — Server membership via invite code

| Truth | Status | Evidence |
|-------|--------|----------|
| ServerMember entity exists with ServerId FK, UserId (Guid), JoinedAt | ✅ | `Pulse.Server/Models/ServerMember.cs` |
| POST /servers/join with valid invite code adds caller as ServerMember; 404 for unknown code | ✅ | `ServersController.JoinServer` |
| GET /servers/me returns all servers the caller belongs to | ✅ | `ServersController.GetMyServers` — queries ServerMembers by callerGuid |
| DashBoard.vue Join Server modal posts to /servers/join | ✅ | `DashBoard.vue joinServer()` |
| DashBoard.vue fetches GET /servers/me on mount | ✅ | `DashBoard.vue onMounted` |

### SRV-12 — Server-scoped rooms and admin identity

| Truth | Status | Evidence |
|-------|--------|----------|
| Room.ServerId is a non-nullable int FK referencing Server; Room.CreatedByUserId is removed | ✅ | `Room.cs` — only `ServerId`, `CreatedByUserId` absent |
| GET /servers/{id}/rooms returns rooms scoped to that server (membership check) | ✅ | `ServersController.GetServerRooms` — Forbid() for non-members |
| POST /servers/{id}/rooms creates a room; 403 for non-owner | ✅ | `ServersController.CreateServerRoom` — checks `server.OwnerId != callerGuid` |
| Old GET /rooms and POST /rooms removed | ✅ | `RoomsController.cs` — only `GetRoomToken` remains |
| IsServerAdmin() / PULSE_ADMIN_USER_ID fully removed from PresenceHub | ✅ | Replaced by `IsOwnerOfServer(int serverId)` — DB-backed check |
| OnConnectedAsync subscribes to server-{serverId} SignalR group for all user's servers | ✅ | `PresenceHub.OnConnectedAsync` — queries `_db.ServerMembers` |
| BroadcastRoomListAsync scoped to server-{serverId} group | ✅ | `Clients.Group($"server-{serverId}")` — no `Clients.All` |
| AssignPrioritySpeaker / RemovePrioritySpeaker use IsOwnerOfServer | ✅ | Both methods check `await IsOwnerOfServer(room.ServerId)` |
| WhisperGroup.ServerId, CreateWhisperGroup(int serverId, ...), all whisper methods use IsOwnerOfServer | ✅ | WhisperGroup record has ServerId; all 4 methods use `IsOwnerOfServer(group.ServerId)` |

### SRV-13 — Client server store and routing

| Truth | Status | Evidence |
|-------|--------|----------|
| useServerStore exists with servers[], activeServerId, activeServer computed | ✅ | `stores/server.ts` |
| usePresence.fetchRooms uses /servers/{activeServerId}/rooms | ✅ | `usePresence.ts fetchRooms` |
| usePresence.createRoom uses /servers/{activeServerId}/rooms | ✅ | `usePresence.ts createRoom` |
| YouAreAdmin listener removed from usePresence | ✅ | No `hubConnection.on('YouAreAdmin', ...)` |
| authStore.isAdmin removed; isAdminOfActiveServer computed from server.ownerId | ✅ | `stores/auth.ts` |
| router beforeEach redirects /server routes to /dashboard when no activeServerId | ✅ | `router/index.ts` |
| ServerTemplate.vue guards onMounted with activeServerId check | ✅ | `ServerTemplate.vue onMounted` |

## Build Verification

| Check | Status |
|-------|--------|
| `dotnet build --no-restore` — 0 C# compiler errors | ✅ |
| `npx tsc --noEmit` — 0 TypeScript errors | ✅ |

## Human Testing Required

| # | Test | Expected |
|---|------|----------|
| 1 | Delete pulse.db, restart server, register + login → Dashboard | Empty state: "No servers yet" with Create and Join CTAs |
| 2 | Create server "Alpha" → Create Server modal | 201 response, navigates to /server, topbar shows "Alpha" |
| 3 | Copy Alpha's invite code; register second user; Join with invite code | User B joins Alpha, navigates to /server |
| 4 | As User A (owner): POST /servers/{id}/rooms to create room | 201 — room appears in room list |
| 5 | As User B (non-owner): attempt POST /servers/{id}/rooms | 403 Forbidden |
| 6 | User A creates a room in Alpha → User B receives RoomListUpdated | User B sees the room (server-scoped broadcast) |
| 7 | Topbar server switcher — click "Alpha" after selecting another server | Dropdown shows server list; click navigates to /server with Alpha active |
| 8 | Navigate to /server without selecting a server (clear store) | Redirected to /dashboard |

## Verification Summary

All 4 plans executed successfully. All must-haves verified via static code inspection. C# builds clean (0 compiler errors). TypeScript builds clean (0 errors). Human testing required to confirm end-to-end flow in running app.
