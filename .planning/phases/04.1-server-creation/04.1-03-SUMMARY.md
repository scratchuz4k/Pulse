---
plan: "04.1-03"
phase: "04.1-server-creation"
status: complete
completed: "2026-06-11"
---

## Summary

Added useServerStore Pinia store, updated usePresence to use server-scoped API endpoints, replaced authStore.isAdmin with isAdminOfActiveServer computed, updated router to guard /server routes, and updated all isAdmin references across the client.

## What Was Built

- `pulse-client/src/renderer/stores/server.ts` — New `useServerStore` with `ServerInfo` interface; `servers[]`, `activeServerId`, `activeServer` computed; `setServers`, `setActiveServer`, `clearServers` actions
- `pulse-client/src/renderer/stores/auth.ts` — Removed `isAdmin = ref(false)`; added `isAdminOfActiveServer = computed(() => activeServer.ownerId === userId.value)`
- `pulse-client/src/renderer/composables/usePresence.ts` — `fetchRooms` uses `/servers/${serverId}/rooms`; `createRoom` uses `/servers/${serverId}/rooms`; removed YouAreAdmin listener; `createWhisperGroup` accepts `serverId` as first parameter; reconnect handler calls updated `fetchRooms`
- `pulse-client/src/renderer/router/index.ts` — `beforeEach` guard: redirect to `/dashboard` when accessing `/server` with no `activeServerId`
- `pulse-client/src/renderer/layouts/ServerTemplate.vue` — Guard in `onMounted`: redirect to `/dashboard` when no `activeServerId`
- `pulse-client/src/renderer/components/WhisperPanel.vue` — `authStore.isAdmin` → `authStore.isAdminOfActiveServer`; `createWhisperGroup` call includes `serverStore.activeServerId!`
- `pulse-client/src/renderer/views/RoomView.vue` — `authStore.isAdmin` → `authStore.isAdminOfActiveServer`

## Self-Check: PASSED

- TypeScript `npx tsc --noEmit`: 0 errors ✓
- `useServerStore` contains `servers`, `activeServerId`, `activeServer` ✓
- `fetchRooms` and `createRoom` use `/servers/${serverId}/rooms` ✓
- `YouAreAdmin` listener absent from usePresence.ts ✓
- `isAdmin` absent from auth.ts (only `isAdminOfActiveServer` present) ✓
- `router/index.ts` contains `useServerStore().activeServerId` guard ✓
- No remaining `authStore.isAdmin` references in renderer/ ✓
