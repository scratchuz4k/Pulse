# Phase 4.1: Server Creation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-11
**Phase:** 4.1-Server-Creation
**Areas discussed:** Invite code behavior, Admin model transition, Multi-server UX, Room scoping + migration

---

## Invite Code Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Static permanent code | One fixed code per server, never expires. Admin regenerates manually if needed. | ✓ |
| Multi-use with expiry | Admin generates codes with optional TTL (24h, 7d, never). | |
| Single-use codes | Each invite link works once, then expires. Requires an Invites table. | |

**User's choice:** Static permanent code

| Option | Description | Selected |
|--------|-------------|----------|
| Immediate join | POST /servers/join — if code matches, user added to ServerMember immediately. | ✓ |
| Pending approval | Join request waits in queue until admin approves. | |

**User's choice:** Immediate join

| Option | Description | Selected |
|--------|-------------|----------|
| Copy code from settings, enter in join modal | Owner copies code from settings; others enter in a join modal. | ✓ |
| Full invite link (URL scheme) | pulse://join/<code> deep link that auto-joins on click. | |

**User's choice:** Copy code from server settings, enter in join modal

---

## Admin Model Transition

| Option | Description | Selected |
|--------|-------------|----------|
| Server owner replaces env var entirely | Server.OwnerId replaces PULSE_ADMIN_USER_ID. Env var and Room.CreatedByUserId removed. | ✓ |
| Keep env var as superadmin override | Env var remains as a fallback god-mode admin in addition to server owner. | |

**User's choice:** Yes — server owner IS the admin, env var removed

| Option | Description | Selected |
|--------|-------------|----------|
| Creator is always owner in v1 | No ownership transfer. POST /servers creator becomes OwnerId permanently. | ✓ |
| Owner can transfer to another member | Add a transfer endpoint. | |

**User's choice:** Creator is always owner in v1

---

## Multi-Server UX

| Option | Description | Selected |
|--------|-------------|----------|
| Users can join/create multiple servers | Data model supports it (ServerMember junction). Client needs switcher. | ✓ |
| Single server only for v1 | One server per user. Simpler client, no switcher needed. | |

**User's choice:** Yes — users can join/create multiple servers

| Option | Description | Selected |
|--------|-------------|----------|
| Left column of server icon buttons (Discord-style) | 48px column left of nav rail. Circular icon buttons, + at bottom. | |
| Dropdown in the header/topbar | Server name in topbar; clicking opens dropdown list of joined servers. | ✓ |
| You decide | Leave layout to Claude's discretion. | |

**User's choice:** Dropdown in the header/topbar

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated 'no server' landing view after login | Landing screen with Create + Join CTAs if user has no servers. | |
| Modal/overlay on the existing view | Normal server view with modal prompting create/join. | |

**User's choice (freeform):** "We can add a join or create a server in the dashboard, that should be the landing page after login."
**Notes:** Dashboard (`/dashboard`) becomes the real landing page. Server creation/join UI lives there. Already the route after auth — just needs to be made functional.

---

## Room Scoping + Migration

| Option | Description | Selected |
|--------|-------------|----------|
| Delete the dev DB and start fresh | Wipe pulse.db and restart. Matches existing AppDbContext dev strategy. | ✓ |
| Assign orphan rooms to the first server | Default ServerId value for existing rooms. | |

**User's choice:** Delete the dev DB and start fresh

| Option | Description | Selected |
|--------|-------------|----------|
| GET /servers/{id}/rooms — scoped endpoint | New route returns only rooms belonging to the selected server. | ✓ |
| Add serverId query param to existing GET /rooms | GET /rooms?serverId={id} filters rooms. | |

**User's choice:** GET /servers/{id}/rooms — scoped endpoint

| Option | Description | Selected |
|--------|-------------|----------|
| Any member can create rooms | POST /servers/{id}/rooms, any server member. | |
| Only server admin can create rooms | Room creation restricted to server owner. | ✓ |

**User's choice:** Only server admin can create rooms

| Option | Description | Selected |
|--------|-------------|----------|
| Subscribe to all my servers on connect | Hub subscribes connection to per-server SignalR groups for all memberships on connect. | ✓ |
| Only the active server — switch explicitly | Client sends "switch to server X"; hub subscribes to only that server. | |

**User's choice:** Yes — subscribe to all my servers on connect
**Notes:** User asked whether cross-server subscription affects visibility of other servers. Confirmed they want presence updates from all servers they belong to simultaneously.

---

## Claude's Discretion

- Exact additional Server entity fields (description, icon/color, createdAt).
- SignalR server group naming convention (e.g., `server-{serverId}`).
- Whether `GET /servers/{id}` single-fetch endpoint is needed in this phase.
- How `activeServerId` is tracked in the client (Pinia store, router state, or both).
- Whether to use `GET /servers/me` for server list or drive from Pinia populated at login.

## Deferred Ideas

- Server icons/avatars — v1 uses name + initials only.
- Ownership transfer — creator is always owner in v1.
- Invite link URL scheme (`pulse://join/<code>`) — plain code copy/paste for v1.
- Member roles beyond owner/member (moderator, etc.) — future phase.
