---
phase: 02-full-room-experience
plan: 05
type: execute
wave: 2
depends_on: ["02-04"]
files_modified:
  - pulse-client/src/renderer/views/RoomView.vue
autonomous: true
requirements:
  - VOICE-06
  - ROOM-02

must_haves:
  truths:
    - "Clicking Deafen sets all remote audio elements to volume 0 and mutes the local mic"
    - "Clicking Undeafen sets all remote audio elements back to volume 1 and restores the previous mic state"
    - "If the mic was enabled before deafening, it is re-enabled on undeafen; if it was muted, it stays muted"
    - "The deafen button shows the active/muted CSS class when deafened"
    - "Speaking ring animation fires on the correct participant (activeSpeakers matched by userId not connectionId)"
  artifacts:
    - path: "pulse-client/src/renderer/views/RoomView.vue"
      provides: "Fixed handleToggleDeafen with prevMicEnabled save/restore; verified speaking ring wiring"
  key_links:
    - from: "handleToggleDeafen"
      to: "prevMicEnabled ref"
      via: "saved before deafen, restored on undeafen"
      pattern: "prevMicEnabled"
    - from: "squad-member .speaking class"
      to: "activeSpeakers.includes(p.userId)"
      via: "v-bind class"
      pattern: "activeSpeakers.includes\\(p\\.userId\\)"
---

<objective>
Fix the deafen un-restore bug and verify speaking indicators work end-to-end.

Purpose: D-05 and D-06 require deafen to be client-side only, but the current handleToggleDeafen does not restore the previous mic state on undeafen (it mutes on deafen but never re-enables on undeafen). ROOM-02 speaking indicators are already implemented but need a quick verification pass to confirm the userId matching is correct.
Output: Fixed deafen toggle with prevMicEnabled save/restore; speaking ring verified against correct identity field.
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projetos/Hopin/.planning/PROJECT.md
@C:/projetos/Hopin/.planning/ROADMAP.md
</context>

<interfaces>
From pulse-client/src/renderer/views/RoomView.vue (current handleToggleDeafen):
  const isDeafened = ref(false)
  async function handleToggleDeafen(): Promise&lt;void&gt; {
    isDeafened.value = !isDeafened.value
    document.querySelectorAll&lt;HTMLAudioElement&gt;('audio[id^="livekit-audio-"]').forEach(el => {
      el.volume = isDeafened.value ? 0 : 1
    })
    if (isDeafened.value && isMicEnabled.value) await toggleMic()
    // BUG: on undeafen, mic is never re-enabled
  }

From pulse-client/src/renderer/composables/useLiveKit.ts:
  isMicEnabled: ref&lt;boolean&gt; — reflects current mic state
  toggleMic(): toggles and updates isMicEnabled

From RoomView.vue squad-member template (current):
  :class="{ speaking: activeSpeakers.includes(p.userId) }"  — uses userId
  sq-speaking-ring uses same .speaking class — correct if userId matches LiveKit identity
  LiveKit identity is set to userId in the token generation (verify: LiveKitService uses userId as identity)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix deafen toggle — save and restore prevMicEnabled</name>
  <files>pulse-client/src/renderer/views/RoomView.vue</files>
  <read_first>
    - pulse-client/src/renderer/views/RoomView.vue (full script setup section — lines 289-380; see handleToggleDeafen and isMicEnabled usage)
  </read_first>
  <action>
    In the script setup section, replace handleToggleDeafen with the following logic:

    Add a module-scope ref: const prevMicEnabled = ref(false)

    Replace handleToggleDeafen:
      async function handleToggleDeafen(): Promise&lt;void&gt; {
        if (!isDeafened.value) {
          // About to deafen: save current mic state, then mute mic
          prevMicEnabled.value = isMicEnabled.value
          isDeafened.value = true
          document.querySelectorAll&lt;HTMLAudioElement&gt;('audio[id^="livekit-audio-"]').forEach(el => {
            el.volume = 0
          })
          if (isMicEnabled.value) await toggleMic()  // mute mic
        } else {
          // About to undeafen: restore audio and restore previous mic state
          isDeafened.value = false
          document.querySelectorAll&lt;HTMLAudioElement&gt;('audio[id^="livekit-audio-"]').forEach(el => {
            el.volume = 1
          })
          if (prevMicEnabled.value && !isMicEnabled.value) await toggleMic()  // restore mic if it was on before
        }
      }

    Also update handleLeave to reset prevMicEnabled: add prevMicEnabled.value = false after isDeafened.value = false.

    NOTE: Do not call broadcastMuteChanged from handleToggleDeafen. Deafen is client-side only (D-07). The toggleMic() call inside handleToggleDeafen triggers handleToggleMic via the button, but the deafen handler calls toggleMic() directly — this means the mute broadcast in handleToggleMic won't fire for the deafen-induced mute. That is CORRECT behavior per D-07 (deafen state is not visible to others). No change needed for this.

    HOWEVER: because toggleMic() is called directly (not via handleToggleMic), the SignalR broadcastMuteChanged will NOT be called for the deafen-mute. This is intentional. Verify this is the case in the existing code and do not add a broadcast call here.
  </action>
  <verify>
    <automated>grep -c "prevMicEnabled" C:/projetos/Hopin/pulse-client/src/renderer/views/RoomView.vue</automated>
  </verify>
  <acceptance_criteria>
    - const prevMicEnabled = ref(false) declared in script setup
    - handleToggleDeafen saves isMicEnabled.value to prevMicEnabled.value before setting isDeafened = true
    - handleToggleDeafen restores mic with if (prevMicEnabled.value && !isMicEnabled.value) await toggleMic() on undeafen
    - audio elements set to volume 0 on deafen and volume 1 on undeafen
    - handleLeave resets prevMicEnabled.value = false
    - broadcastMuteChanged is NOT called from handleToggleDeafen (deafen is silent to others per D-07)
  </acceptance_criteria>
  <done>Deafen saves and restores previous mic state; undeafen correctly re-enables mic if it was on before deafening.</done>
</task>

<task type="auto">
  <name>Task 2: Verify speaking indicators — inspect and fix if userId mismatch found</name>
  <files>pulse-client/src/renderer/views/RoomView.vue</files>
  <read_first>
    - pulse-client/src/renderer/views/RoomView.vue (template section, squad-member divs — lines 1-287)
    - pulse-client/src/renderer/composables/useLiveKit.ts (see how activeSpeakers.value is populated — uses speaker.identity)
    - pulse-client/src/renderer/stores/room.ts (see Participant.userId — is this the same as LiveKit identity?)
  </read_first>
  <action>
    Inspect the speaking ring wiring:

    In useLiveKit.ts: room.on(RoomEvent.ActiveSpeakersChanged, (speakers) => { activeSpeakers.value = speakers.map(s => s.identity) })
    LiveKit identity = the identity string in the token. In LiveKitService.GenerateRoomToken, the identity is set to the userId string.
    In the store: Participant.userId is the claim value from the JWT, same as what was passed to GenerateRoomToken.
    Therefore: activeSpeakers contains userId strings, and p.userId on Participant is the same value.

    Verify in the template that both squad panels use activeSpeakers.includes(p.userId) (not p.connectionId) for:
      - The .speaking CSS class on squad-member div
      - The v-if for sq-speaking-ring span
      - The vb-av.speaking class in the bottom bar

    If any of these use p.connectionId instead of p.userId, correct them to use p.userId.

    Current state from reading the file:
    - Voice tab squad-member: :class="{ speaking: activeSpeakers.includes(p.userId) }" — CORRECT
    - Voice tab sq-speaking-ring: v-if="activeSpeakers.includes(p.userId)" — CORRECT
    - Hub tab squad-member: :class="{ speaking: activeSpeakers.includes(p.userId) }" — CORRECT
    - Bottom bar vb-av: :class="{ speaking: activeSpeakers.includes(p.userId) }" — CORRECT

    If all are correct (identity-based matching), no template changes needed. Add an inline comment in the template next to the speaking ring:
      &lt;!-- ROOM-02: sq-speaking-ring visible when p.userId matches LiveKit speaker identity --&gt;

    If any mismatch is found, fix it to use p.userId.

    Also verify the speakerName computed works correctly (it looks up by userId already).

    This task is primarily a verification task. If no bugs are found, the only output is the inline comment.
  </action>
  <verify>
    <automated>grep -c "activeSpeakers.includes(p.userId)" C:/projetos/Hopin/pulse-client/src/renderer/views/RoomView.vue</automated>
  </verify>
  <acceptance_criteria>
    - All four uses of activeSpeakers in the template use p.userId (not p.connectionId) — grep count is at least 4
    - Inline comment added near sq-speaking-ring confirming ROOM-02 identity matching
    - No change to the speaking ring CSS or animation (it is already correct)
  </acceptance_criteria>
  <done>Speaking indicators confirmed to use userId matching LiveKit identity; deafen fix complete; ROOM-02 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer audio elements | All client-side; no server trust boundary for deafen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02E-01 | Information Disclosure | Deafen state visible to others | accept | D-07 explicitly says deafen is not broadcast; this is by design |
| T-02E-02 | Spoofing | Client re-enables audio elements manually via devtools | accept | Client-side audio control is client's own concern; no security implication |
</threat_model>

<verification>
Manual smoke test:
1. Client A and Client B are in the same room, both speaking.
2. Client A clicks Deafen. Confirm: deafen button turns red/muted style. Client A's mic button also shows muted.
3. Client B speaks — Client A hears nothing (volume 0). Client B's speaking ring is still visible in Client A's UI.
4. Client A clicks Undeafen. Confirm: audio from Client B is audible again.
5. Check mic state: if Client A's mic was enabled before deafening, it should be enabled again. If it was muted, it stays muted.
6. With Client A's mic on: deafen → mic shows muted. Undeafen → mic shows enabled again.
7. With Client A's mic off: deafen → mic stays off. Undeafen → mic stays off.
8. ROOM-02: Both clients speak simultaneously — speaking ring animation appears around each active speaker's avatar.
</verification>

<success_criteria>
- Deafen silences incoming audio and mutes local mic
- Undeafen restores audio and restores previous mic state
- Speaking rings fire for active speakers (ROOM-02 end-to-end verified)
</success_criteria>

<output>
Create .planning/phases/02-full-room-experience/02-05-SUMMARY.md when done
</output>
